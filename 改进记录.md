# 改进记录

> 智能零售分析系统自 **v3.0（2026-08-19 提交）** 以来的全部改进记录。
> 每项按「**问题 → 做法 → 验证 → 收益**」记录：既作维护参考，也作面试复盘材料。
>
> **维护约定**：后续每次项目改动都追加到本文档——在对应分类下新增小节，
> 并同步更新下方索引表。**新改进请务必写清「验证方式」**（数字/断言数/现象），
> 这是本文档区别于 README 的价值所在。
>
> 相关文档：`README.md`（安装与使用）、`模板路由劫持防御_演进记录.md`（路由层专项）、
> `并发压测报告_方法与结果.md`（压测专项）、`亮点二_多摄像头模块化与地图.md`（多摄像头与地图专项）

---

## 索引

| # | 改进项 | 类别 | 主要文件 |
|---|---|---|---|
| 1 | 登录鉴权 + root/platform 双角色 | 架构/安全 | `api/security.py`、`api/routes/auth.py` |
| 2 | 汇报链路改 LangGraph StateGraph | 架构 | `agents/report_agent.py` |
| 3 | Qdrant 嵌入式 → Server 模式 | 架构 | `vector_memory.py` |
| 4 | 数据可信度门禁 | 业务正确性 | `agents/data_quality.py` |
| 5 | 同期对比（昨天/上周） | 业务能力 | `agents/period_compare.py` |
| 6 | 销量来源标注 + 真实接入 | 业务正确性 | `agents/sales_ingest.py` |
| 7 | 销量目录自动同步（POS 投递） | 业务能力 | `agents/sales_inbox.py` |
| 8 | 主动汇报 WS 订阅闭环 | 前端/链路 | `frontend-vue/src/App.vue` |
| 9 | 网页登录页 + 登录门禁 | 前端 | `Login.vue`、`Root.vue` |
| 10 | 小程序登录（Bearer 适配） | 多端 | `miniprogram/utils/api.js` |
| 11 | 压测分位单位错标修复 | 缺陷修复 | `benchmark/loadtest.py` |
| 12 | 人脸检测 `faces=None` 崩溃修复 | 缺陷修复 | `cv_engine/video_processor.py` |
| 13 | 首次启动无法登录（lifespan 顺序） | 缺陷修复 | `api/main.py` |
| 14 | 多路视频真并行（per-camera detector） | 性能 | `cv_engine/multi_stream.py` |
| 15 | 地图工具 TTL 缓存 | 性能/成本 | `agents/map_tools.py` |
| 16 | 摄像头配置持久化 | 可运维 | `agents/module_registry.py` |
| 17 | 评测 CI 门禁 | 质量保障 | `evals/run_evals.py`、`.github/` |
| 18 | 深度复盘运营档案 | 业务能力 | `agents/ops_archive.py` |
| 19 | 人脸脱敏合规开关 | 合规 | `cv_engine/face_emotion.py` |
| 20 | SSE 流式回答（小程序端） | 体验 | `miniprogram/pages/index/index.js` |
| 21 | 本机摄像头低延迟采集 | 体验 | `frontend-vue/src/App.vue` |
| 22 | 移除原生 JS 前端，Vue 唯一化 | 维护性 | `frontend/`（删除） |
| 23 | 配置外置与环境变量清单 | 可复现 | `README.md`、`.env.example` |
| 24 | `.gitignore` 补全（防误提交） | 仓库卫生 | `.gitignore` |
| 25 | **登录安全加固（6 项修复）** | 安全 | `api/security.py`、`api/routes/auth.py`、`api/main.py`、`config/settings.py` |
| 26 | **`.env` 加载修复（文档与实现不符）** | 缺陷修复 | `config/settings.py`、`.env.example` |
| 27 | **令牌不进 URL：一次性票据（B）+ 服务端收紧（C）** | 安全 | `api/security.py`、`api/routes/auth.py`、`miniprogram/` |
| 28 | **销量来源改为显式 `source` 字段（不再靠前缀猜）** | 数据正确性 | `mysql_db.py`、`agents/sales_ingest.py`、`agents/sales_analytics.py`、`agents/traffic_analytics.py` |
| 29 | **容器化（Dockerfile + Docker Compose）** | 交付/可复现 | `Dockerfile`、`docker-compose.yml`、`.dockerignore`、`README.md` |

> **关于编号 30 及以后**：它们是在「容器化 + 系统加固」这一轮里陆续发现并修掉的，
> 正文按 `#### 29.x` 的子章节记录在**第十部分**（该部分的标题已相应改为
> 「容器化交付**与运行时可靠性加固**」，因为 29.14 之后的内容已与容器化无关）。
> 索引里的「见 29.N」就是精确的正文位置。这样编号只增不改，
> 外部（README / 简历 / 面试材料）引用过的编号不会失效。
| 30 | compose 漏传变量导致地图/langfuse 静默失效（+ `.env` 机制） | 缺陷修复/配置 | `docker-compose.yml`、`.env`（见 29.7） |
| 31 | **langfuse 依赖未锁上界 → trace 静默失效** | 缺陷修复 | `requirements.txt`（见 29.8） |
| 32 | 构建提速 8.6 倍（torch `--no-deps`） | 构建性能 | `Dockerfile`（见 29.9） |
| 33 | **向量语义层容器内失效（硬编码 127.0.0.1）** | 缺陷修复 | `vector_memory.py`、`docker-compose.yml`（见 29.10） |
| 34 | 镜像曾含 27.5MB 业务数据（`.dockerignore` 漏 `*.sql`） | 安全/交付 | `.dockerignore`（见 29.13） |
| 35 | 游离容器清理 + Redis 常驻 + 统一入口 `stack.ps1` | 可运维 | `docker-compose.yml`、`stack.ps1`、`README.md`（见 29.11 / 29.12） |
| 36 | **就绪探针：破解"假健康"** | 可运维/可靠性 | `api/main.py`、`api/security.py`、`Dockerfile`（见 29.14） |
| 37 | 日志轮转 + 生产路径澄清 + 第三方评估核对 | 可运维 | `docker-compose.yml`、`README.md`（见 29.14 / 29.15） |
| 38 | **`chat.py`/`emotion_camera.py` 阻塞事件循环** | 性能/可用性 | `api/routes/chat.py`、`api/routes/emotion_camera.py`（见 29.16） |
| 39 | **处理线程异常静默死亡（画面永久卡死）** | 可靠性 | `api/routes/stream.py`（见 29.17） |
| 40 | **ROI 并发改写导致处理线程抛异常** | 并发正确性 | `cv_engine/roi_manager.py`（见 29.17） |
| 41 | **连接池满 → 无上限裸连接（可拖垮全库）** | 可靠性 | `db_engine.py`、`mysql_db.py`（见 29.18） |
| 42 | **两个用户同时开「本机摄像头」互相串台** | 并发正确性 | `api/routes/stream.py`（见 29.19） |
| 43 | **SQLite 降级路径表没建 → 数据静默丢失** | 数据可靠性 | `database.py`（见 29.20） |
| 44 | **MySQL 掉线时每次 DB 调用白等 8 秒（放大成全站卡死）** | 可用性 | `db_engine.py`、`mysql_db.py`（见 29.21） |
| 45 | **同会话并发写入全部撞成同一个 seq_no** | 并发正确性 | `mysql_db.py`（见 29.22） |
| 46 | **多路常驻引擎 `_retired` 无界增长** | 资源管理 | `cv_engine/tracker.py`（见 29.23） |

---

# 一、架构与安全

## 1. 登录鉴权 + root/platform 双角色

**问题**
系统所有 `/api/*` 完全开放：任何人知道地址就能读业务数据、**覆写销售数据**（销量导入接口可按任意时段 UPSERT）、生成演示数据、管理摄像头。作为「面向门店落地」的系统，缺鉴权是生产前提性缺陷。

**做法**

- **零新增依赖**：口令哈希用标准库 `hashlib.pbkdf2_hmac`（PBKDF2-HMAC-SHA256 + 每用户 16 字节随机盐 + 12 万轮），常量时间比较防时序侧信道
- **会话存服务端**（SQLite `data/auth.db`）：改密码/改角色/停用即**即时吊销**旧会话，不需要 JWT 黑名单
- **独立于业务库**：鉴权数据不用 MySQL——**业务库故障时不应把管理员也锁在门外**，否则无法登录排查
- **双通道令牌**：浏览器走 HttpOnly Cookie（同源请求与 WebSocket 握手自动携带，**前端零改动**）；小程序/脚本走 `Authorization: Bearer` 或 `?token=`
- **鉴权中间件用纯 ASGI 实现**——`BaseHTTPMiddleware` **不拦截 WebSocket**，视频流/汇报推送这类 WS 通道会绕过鉴权
- **默认封启**：`/api/*` 一律需登录；白名单仅登录/登出/健康检查/前端回退上报/接口文档，静态资源与 SPA 入口放行（否则登录页都加载不出来）
- **首次启动引导**：无账号时自动创建 root，随机密码打印到启动终端；可用 `AUTH_ROOT_PASSWORD` 指定

**角色权限矩阵**

| 权限 | root（平台管理员） | platform（平台账户） |
|---|---|---|
| `data:read` 查看业务数据 | ✅ | ✅ |
| `data:write` 导入销量等写操作 | ✅ | ✅ |
| `user:manage` 用户管理 | ✅ | ❌ |
| `system:manage` 系统管理 | ✅ | ❌ |

**保护机制**：不能删/停用/降级**最后一个可用 root**；不能删/停用自己；platform 无法自提权（建 root 账号 → 403）。

**验证**

| 层 | 项数 | 关键断言 |
|---|---|---|
| 单元 | 44/44 | 哈希非明文、随机盐、错误口令/损坏哈希不抛异常；会话过期/改密码/停用后立即失效；权限矩阵；最后一个 root 保护；用户名密码校验 |
| HTTP/WS | 43/43 | 未登录 401；登录发 HttpOnly Cookie；Bearer 与 `?token=` 通道；**platform 查用户列表 403**、建账号 403；root 用户增删改；不能删/停用自己；登出后 token 失效；**未授权 WS 被拒（1008）/ 授权 WS 正常** |
| 引导 | 13/13 | 首次启动创建 root、密码哈希落库、二次启动不重复创建、`AUTH_ROOT_PASSWORD` 生效 |

**收益**：从「完全开放」到「默认封启 + 最小权限」；写操作（销量导入）不再可被匿名覆写。

---

## 2. 汇报链路改 LangGraph StateGraph

**问题**
主动汇报链路（定期汇报 + 异常突增推送）此前是**函数式硬编码 + while 循环**：采数据、生成文案、降级兜底全揉在几个函数里，**没有显式的流程结构**——分支不可见、降级路径不明确、难以回溯与调试。

**做法**
用 LangGraph `StateGraph` 显式编排，含**真实条件分支**：

```
START → collect → generate ──LLM 成功──→ sanitize → finalize → END
                          └──LLM 失败──→ template ↗
```

| 节点 | 职责 |
|---|---|
| `collect` | 采集热度/告警/表情快照 + 数据可信度快照 |
| `generate` | 调用 LLM 生成自然语言汇报 |
| `template` | **降级分支**：LLM 不可用时模板拼接，保证链路不断 |
| `sanitize` | 合规过滤（法律定性词 → "可疑行为"） |
| `finalize` | 组装结构化 payload |

**关键约束**：对外接口（`generate_regular_report` / `generate_surge_report` / `detect_surge` / `update_baseline` / `reset_baseline`）**签名与返回结构一字未改**，调用方 `scheduled_tasks.py` 无需改动——重构不改变契约。

**验证**

- 22 项断言：图结构节点齐全；LLM 成功分支走 sanitize（合规过滤生效）；LLM 失败分支条件边正确转 template；regular/surge payload 字段集与旧版逐一对齐；surge 告警数取自入参而非 anom；基线行为不变；`scheduled_tasks` 可正常导入
- **真实端到端**（无打桩）：真实 skill 加载（5 个 ROI 区域）+ 真实 DeepSeek 调用 → 走 LLM 分支、结构正确

**收益**：汇报链路有了显式图结构——分支可读、降级路径明确、可回溯可 Debug；也为后续「变化驱动汇报」等条件分支预留了位置。

---

## 3. Qdrant 嵌入式 → Server 模式

**问题**
`vector_memory.py` 原本用 Qdrant **嵌入式模式**（数据存本地 `qdrant_data/`，67 MB）。README 自己写明：**嵌入式模式不支持多进程同时打开同一向量库目录**——这成为「多进程 / 多机部署」的结构性拦路虎。

**做法**
代码本就支持双模式（`if QDRANT_URL:` 走 Server，否则走嵌入式），因此**代码零改动**，只需：
1. 启动 Qdrant Server 容器（`qdrant/qdrant`，端口 6333）
2. 设置 `QDRANT_URL=http://127.0.0.1:6333`（已固化为用户级环境变量）

**验证**

| 项 | 结果 |
|---|---|
| 客户端模式 | `Server`（命中 `if QDRANT_URL` 分支，非嵌入式） |
| 连接与数据 | 读到 Server 中 `query_history_vectors` **4388 点** |
| 端到端召回 | 3 个真实业务问题均召回成功，最高相似度 **0.8493**（Cosine 打分正常） |

**收益**：解锁多进程/多机能力；切换**未影响向量召回质量**。

> 遗留：一旦真正多进程，进程内状态（`_MAP_CACHE`、`asyncio.Semaphore` 限流、`_active_camera`、`_baseline`）会各自为政，需外置（Redis 等）——见「后续待办」。

---

# 二、业务正确性

## 4. 数据可信度门禁

**问题（实测复现）**
摄像头未启动 / 断流 / 离线时，各 skill 的统计值**仍然是 0**。系统把 0 直接当业务事实输出：

> 「各货架到访均为 0 人次，停留 0.0 秒，热度 0.0…建议确认设备运行是否正常」

**最终那句"设备可能异常"是 LLM 自己猜的，不是系统判断的**——系统里根本没有"数据是否可信"这个概念（全项目搜"健康"都是指业务健康象限，没有一个指设备/数据健康）。

**业务风险**：店长读到的是「**今天没生意**」而不是「**摄像头坏了**」；自动汇报照常推送、Agent 照常归因——**基于假数据给出运营建议**。

**做法**

- 新增 `agents/data_quality.py`：各帧处理入口（零售线程 / 表情线程 / 客户端推帧通道）上报「最后一次成功出帧时刻」
- 按 `DATA_STALE_SECONDS`（默认 90s，可配置）判定三种状态：**未启动**（无任何帧）/ **断流**（超阈值）/ **正常**
- 多路场景：**任一路新鲜即整体可信**，其余单独标 `stale`
- 汇报链路接入：数据不可信时 **summary 明确标注原因，且不输出「到访 0 人次」这类误导结论**；payload 新增 `data_quality` 字段
- **LLM prompt 也注入可信度说明**，明确禁止它把 0 解读成业务结论
- 新增 `GET /api/reports/data-quality`

**验证（18 项断言全通过）**

| 组 | 关键断言 |
|---|---|
| 状态机 | 无帧 → `reliable=False`（原因"未启动"）；刚出帧 → `True`；伪造 100s 前帧 → `False`（原因"断流"）；`warning_text` 给出提示 |
| 汇报链路 | 不可信时 summary 含"数据不可信"、**不含"累计到访"式误导**；payload 带 `data_quality`；surge 不可信时同样标注且保留 delta 字段 |
| 无回归 | **可信时行为与改造前一致**（恢复模板业务文案）；原有 `type`/`generator`/`stats` 字段仍在 |
| 多路 | 一路新鲜 → 整体可信；已断的那一路单独标 `stale` |

**收益**：**不再把设备故障输出成业务结论**——这是任何数据分析系统的可信度底线。

---

## 5. 同期对比（昨天 / 上周同期）

**问题**
系统此前把「昨天客流多少」「比上周怎么样」这类问题**直接交给 LLM 并回答"系统只有实时数据"**（`intent_router.py` 里明确把过去时间限定路由给 LLM，注释写着"需模型诚实说明无历史"）。

**但数据其实存了**：`retail_stats` / `product_sales` 按 `period_key(YYYYMMDDHH)` 写入，`DB_RECORD_KEEP_DAYS=30` 留存 30 天。**数据在库里，只是缺查询入口。**

**业务价值**：单点数字没有决策价值——"今天客流 120"是无用信息，"**比上周同期跌 30%**"才是行动信号。店长每天问的第一句话就是"比昨天好还是差"。

**做法**

- 新增 `agents/period_compare.py`：复用 `mysql_db` 现成的「按 period_key 聚合」接口（**不新增 SQL**），对「当前时段」与「昨天同期（-24h）/ 上周同期（-168h）」做**同口径对比**（period_key 是整点，偏移均为 24 的倍数，避免拿今天 1 小时比昨天全天）
- 输出差值、变化率（基数为 0 时返回 `None` 而非编造）、可读结论；**±5% 内视为持平**（抑制噪声抖动）
- 注册为 Agent 工具 `get_period_comparison` + SYSTEM_PROMPT 规则 22，并**明确禁止回答"没有历史数据"**
- 新增 `GET /api/analytics/period-compare`

**验证（38 项断言全通过）**

| 组 | 关键断言 |
|---|---|
| 口径 | `period_key` 为 `YYYYMMDDHH`（与视频管线一致）；昨天/上周 key 计算正确 |
| 计算 | 打桩数据下差值 +20、变化率 +25%、上周 +100% 全部正确 |
| 边界 | 下降判负、小幅波动判持平、基数为 0 → 「无对比基线」、**无数据时不编造结论** |
| 工具 | 已注册进 `self.tools`、可调用返回 JSON、SYSTEM_PROMPT 有规则 22 且禁止"无历史"式回答 |
| **回归** | **本轮未改 `intent_router`**：评测集仍 80 条、router 用例仍 31 条、4 条典型路由结果不变 |

**收益**：单点数据变成**可决策的趋势对比**；把一条"只能回答没有历史"的死路变成可用能力。

---

## 6. 销量来源标注 + 真实接入

**问题**
转化率四象限、动线价值等归因能力**全部依赖销量**，但项目提供的是 `simulate_demo_sales()` 生成的**演示数据**（固定 `period_key="demo"`），而系统**既不区分、也不标注数据来源**——**在演示数据上得出的"高热度低销量"结论只是功能演示，却被当作经营结论展示**。

**做法**

- 新增 `agents/sales_ingest.py`：
  - **来源判定**：`period_key` 以 `demo` 开头 → `simulated`，其余 → `pos`（沿用既有命名，**无需改表结构**）
  - **真实导入**：`import_sales()` 写入整点口径 `YYYYMMDDHH`（与视频管线一致 → 可参与同期对比）；支持 JSON 批量与 **CSV 解析**（表头可带可省、支持 `#` 注释与空行、错误带行号）
  - **防呆**：拒绝把真实数据写进 `demo*` 命名空间
- `compare_hotness_vs_sales` 输出新增 `sales_source`（pos/simulated/mixed/none）与 `data_caveat`——**演示数据时明确写明"结论仅用于功能验证，不可作为经营决策依据"**
- 新增 `mysql_db.get_sales_period_keys()`（DISTINCT 查询，仅用于来源判定）
- 新增 `POST /api/analytics/sales/import`（JSON records 或 CSV 文本）

**验证**

| 层 | 项数 | 关键断言 |
|---|---|---|
| 单元 | 39/39 | 来源判定（demo→simulated、整点→pos、混合→mixed）；CSV 解析与 4 类非法输入拒绝；拒绝写 demo* 命名空间；归因结果带 `sales_source`/`data_caveat` 且原字段未破坏 |
| **真实写入回读** | 全通过 | `import_sales` → MySQL → 读回判定 `pos` → 归因 `sales_source=pos`、`caveat` 为空 |
| **HTTP 契约** | 16/16 | CSV 导入 200；records 导入 200 且 UPSERT 覆盖；csv+records 同时给 → 400；都不给 → 400；CSV 数值非法 → 400 **带行号**；demo* → 400 |

**收益**：归因结论**不再冒充真实数据**；同时打通真实销量接入通道，为后续 POS 对接留好接口。

> **诚实边界**：这是**真实销量的接入通道**（接口 + 口径 + 来源标注），**不是"已对接 POS"**。生产上对接 POS 通常走数据库直读 / 厂商 API / 定时文件投递。

---

## 7. 销量目录自动同步（POS 投递形态）

**问题**
上一项的导入是「**人手调接口**」形态，而真实零售里销量数据本来就在 POS 里，正确形态是**系统定时对接**，不是人来点。

**做法**
新增 `agents/sales_inbox.py`，对齐真实零售常见形态（POS/ERP 定时导出到共享目录）：

```
POS/ERP 定时导出 CSV → data/sales_inbox/
                          ↓ 后台线程每 60s 扫描
                    解析 → 导入（整点口径 + 来源标记 pos）
                          ↓
              成功 → processed/（归档）   失败 → failed/（保留文件便于排查）
```

| 特性 | 实现 |
|---|---|
| 触发 | 后台线程自动（可配周期）+ `POST /api/analytics/sales/scan-inbox` 手动触发 |
| period_key | 文件名含 10 位数字（`sales_2026091314.csv`）→ 采用；否则当前整点 |
| **幂等** | 文件处理后即被移走，同一文件不会重复导入 |
| **坏文件隔离** | 移到 `failed/`，**不影响同批次其他文件** |
| BOM 兼容 | `utf-8-sig`（Windows/POS 导出常见） |
| 可禁用 | `SALES_INBOX_ENABLED=0` |

**验证**

| 组 | 关键断言 |
|---|---|
| 文件名解析 | 10 位取到；8 位日期不误取 |
| 正常导入 | 扫到 → 导入 → 归档 `processed/` → inbox 原文件消失；真实落库回读值正确 |
| **幂等** | 再次扫描 `scanned=0`，**数据未被放大** |
| **坏文件隔离** | 坏文件进 `failed/` 且错误带行号，**好文件照常导入** |
| 其他 | 带 BOM 文件正常导入；空目录为空操作；HTTP 触发 200 且幂等；worker 首次返回线程、重复调用返回 `None`、开关关闭不启动 |

**收益**：销量接入从「人手点」变成「**机器投递**」；同时具备幂等与坏文件隔离，可无人值守。

---

# 三、前端与多端

## 8. 主动汇报 WS 订阅闭环

**问题**
后端实现了完整的主动汇报推送链路——`/api/ws/reports` 端点、订阅集合 `_report_clients`、`broadcast_report()` 跨线程广播——**但没有任何客户端订阅它**。`broadcast_report()` 每次都是「广播给 0 个订阅者」，前端实际用 15 秒 REST 轮询取最新一条。**链路后端完整、前端未接**。

**做法**（`frontend-vue/src/App.vue`）

- 新增 `connectReportWs()` 订阅 `/api/ws/reports`；**保留 REST 轮询作为兜底**（WS 断线期间仍有数据，零回归风险）
- **退避重连**：3s 起、每次 +3s、最长 30s
- **保活心跳**：每 60s 发 ping——汇报间隔 10 分钟，长空闲会被中间代理（内网穿透/反代）断开
- 抽出公共渲染函数 `updateReportBar()`：REST 与 WS 共用，避免逻辑漂移
- `beforeUnmount` 完整清理：置关闭标记阻止重连 + 清定时器 + 断连

**验证（7 项断言全通过）**

| 断言 |
|---|
| WS 握手 + 订阅者注册 |
| `broadcast_report` 跨线程投递到订阅者 |
| 载荷 `type/summary/data.type/ts` 与前端读取方式一致 |
| 时间格式化正确（`2026-09-13 11:55:00`） |
| 保活 ping 后连接不断；第二条推送仍可收到 |
| 断开后订阅者清理（clients=0） |
| 构建产物含 `ws/reports` + 两个方法名 |

**收益**：汇报**秒级即时显示**，不再等 15 秒轮询；断线仍有轮询兜底。

---

## 9. 网页登录页 + 登录门禁

**做法**

- `Login.vue`：账号密码登录页（**无验证码/手机号/微信**），含错误提示、加载态、自动聚焦；区分 401（凭据错）与其他错误
- `Root.vue`：**登录门禁根组件**——启动问 `/api/auth/me`，401 → 登录页，200 → 加载 `voice.js` 后挂载仪表盘；`main.js` 改挂 `Root`
- `App.vue`：顶栏加**用户名 + 角色标识**与**退出**按钮（退出吊销服务端会话并整页重载，彻底清掉 WS 连接）

**为什么用 `Root.vue` 而不是改 `App.vue`**：`App.vue` 是 1300+ 行的整个仪表盘，在它**外层**做门禁可以做到「**未登录完全不挂载仪表盘**」——受保护的视频 WS 就不会在未登录时反复重连。（项目没有 vue-router，也不为此引依赖。）

**收益**：登录态对用户可见可控；未登录不再触发受保护通道的重连风暴。

---

## 10. 小程序登录（Bearer 适配）

**问题**
后端默认封启后，小程序**完全不可用**：`utils/api.js` 用 `wx.request`，**不携带 Cookie**，所有请求 401。

**核心设计：接口不分端，只适配传输层**

| | 浏览器端 | 小程序端 |
|---|---|---|
| 携带方式 | HttpOnly Cookie（自动） | **`Authorization: Bearer <token>`** |
| WS / 音频 URL | Cookie 自动带 | **令牌放 query**：`?token=xxx` |

后端本就支持三种通道（`token_from_request` / `token_from_scope`），因此**后端零新增**。

**改动**

| 文件 | 改动 |
|---|---|
| `app.js` | 登录态：`setAuth`/`clearAuth`/`isLoggedIn`/**`hasPerm`**（按角色显隐 UI） |
| `utils/api.js` | 统一注入 Bearer；**401 → 清登录态并回登录页**；403 → 提示权限不足；新增 `login/logout/me`、**`wsUrl()` 自动附 token** |
| `pages/login/*` | 新增登录页（js/wxml/wxss/json）；注册为**首页**，已登录自动跳主页 |
| `pages/index/index.js` | SSE 流式请求加鉴权头 + 401 跳登录 |
| `pages/video/video.js` | WS 改用 `api.wsUrl('/api/ws/stream')` |
| `pages/voiceask/voiceask.js` | ASR WS 带 token；**TTS 音频 URL 补 `&token=`** |

> **易漏点**：TTS 是交给音频播放器**按 URL 播**的——**音频元素无法发送请求头**，必须走 `?token=`，否则语音回复会 401。

**验证**

| 组 | 项数 | 关键断言 |
|---|---|---|
| 静态 | 6 文件 | `node --check` 全过；`app.json`/`login.json` 合法，首页 = `pages/login/login` |
| **逻辑（Node 加载真实模块，桩掉 wx/getApp）** | 17/17 | 无 token 不带 `Authorization`、有 token 带 Bearer、**空串不产生 `"Bearer "` 空令牌**；`wsUrl` 的 http→ws / https→wss / 已有 query 用 `&` / 无 token 不加参 / **不出现 `/api/api`**；`onLaunch` 恢复持久化登录态；**platform 无 `user:manage`、root 有**；存储为空时降级不抛异常 |
| **协议（对真实应用）** | 13/13 | 无 Cookie 登录拿 token；全新客户端无凭据 401 → 带 Bearer **200**；平台账户访问用户管理 **403**；**TTS 无 token 401 / 带 `?token=` 200 且返回真实音频**；**WS 无 token 被拒 / 带 `?token=` 正常**；令牌吊销后两种通道都失效 |

**收益**：小程序恢复可用且与网页端**共用同一套 API**（不按端复制接口）。

---

# 四、缺陷修复

## 11. 压测分位单位错标

**问题**
`benchmark/loadtest.py` 的 `percentile()` 返回的是**秒**，但报告模板给它硬加了 `"ms"` 后缀；而 `avg_ms`/`min_ms`/`max_ms` 都乘了 1000。结果报告出现**自相矛盾**的数据：

| 指标 | 报告所示（错） | 真实值 |
|---|---|---|
| 平均 | 576.32ms | 576.32ms |
| P50 | ~~0.551ms~~ | **551.0ms** |
| P99 | ~~1.082ms~~ | **1082.0ms** |
| 最大 | 1090.19ms | 1090.19ms |

**P99（1.082ms）小于平均值（576.32ms）——数学上不可能**。任何懂行的人扫一眼就会质疑整个压测的可信度。

**做法**
`percentile` → `percentile_ms`，内部统一 ×1000 换算为毫秒，与 `avg_ms`/`min_ms`/`max_ms` 同单位；docstring 记录该历史 bug 成因。

**验证**

- 合成数据：`min ≤ P50 ≤ P90 ≤ P95 ≤ P99 ≤ max` 单调性通过
- 旧报告数据复算：`avg=576.32ms / P50=551.0ms / P90=825.0ms / P95=1069.0ms / P99=1082.0ms / max=1090.19ms` **完全自洽，且 P99≈max**
- 真实重跑：`P50 1524.55ms / P99 2014.72ms / max 2071.42ms`，与平均 1530.14ms 同量级

**收益**：数据从"自相矛盾"变为"专业可信"。**注意：优化前后的倍数关系（QPS 6 倍、P99 12 倍）不受影响**——因为前后同单位错标，相除时相互抵消。

---

## 12. 人脸检测 `faces=None` 崩溃

**问题**
服务器摄像头运行约 4 帧后卡死。根因：`cv_engine/video_processor.py` 中 `for face in faces` 遇到 `faces=None` 抛 `TypeError: 'NoneType' object is not iterable`。

**做法**：遍历前加 `faces = faces or []` 判空。

**验证**：编译通过；服务器摄像头不再 4 帧卡死。

---

## 13. 首次启动无法登录（lifespan 顺序错误）

**问题（本次鉴权改造中发现）**
`api/main.py` 的 lifespan 里先调 `purge_expired_sessions()`（清过期会话）**再**调 `bootstrap_root()`。但**全新环境里 `auth.db` 还没建表**，purge 直接抛异常 → 被 try/except 吞掉 → **`bootstrap_root()` 根本没执行 → root 账号不存在 → 谁都进不去系统**。

**为什么危险**：这是一条**只有"全新部署"才会走到**的路径——日常开发（已有 auth.db）完全看不出来；而**恰好是面试官 clone 下来第一次启动时的路径**。

**做法**：调整为 `init_auth_db()`（建表）→ `bootstrap_root()` → `purge_expired_sessions()`，并加回归断言。

**验证**：独立测试库上复刻 lifespan 顺序，断言"**全新库上 purge 不抛异常**"、随后引导创建 root 成功。

---

# 五、性能、可运维与质量保障

## 14. 多路视频真并行（per-camera detector）

**问题**
`MultiStreamEngine` 各路共享一个检测器并加锁 → 多路被**串行化**，一路慢拖累全部。

**做法**：改为**每路独立 `YOLODetector`**，去掉共享锁；`_open()` 的 RTSP 分支改用 `cv2.VideoCapture(s)`（不做 `int()` 转换），webcam/数字用 `CAP_DSHOW`。

**收益**：多路真正并行；RTSP 源可用。

---

## 15. 地图工具 TTL 缓存

**问题**
百度地图 API 调用（地理编码/距离）**重复率高且有配额**，每次都回源浪费配额。

**做法**：`agents/map_tools.py` 加 `_MAP_CACHE`（dict + TTL 3600s + 上限 500）+ `_cache_get/_cache_set`，`batch_geocode`/`calc_distances` 走缓存。

**收益**：降配额消耗 + 提速。

> **已知待改进**：缓存满时是 `clear()` **一次性全清**（500 条同时作废 → 缓存雪崩，一批请求同时回源）。正确做法是按过期时间淘汰最旧的少量条目。**注意这不是 Redis 该解决的问题**——单进程下内存字典足够；只有多进程才需要 Redis 做共享缓存。

---

## 16. 摄像头配置持久化

**问题**
管理接口运行时增删摄像头/开关模块后，**配置只存在内存**，重启即丢。

**做法**：`agents/module_registry.py` 加 `save_to_yaml()` 写回 `config/cameras.yaml`；`api/routes/cameras.py` 的 add/remove/set_enabled 成功后调用。另：`_active_camera` 默认改为 `None`（此前默认 `cam_in_01`）；`add_camera` 的 cam_id 用**最大数字后缀递增**（避免删除后碰撞）；`/active` 改用 body。

**收益**：运行时配置变更**重启后仍生效**，可真正运维。

---

## 17. 评测 CI 门禁

**问题**
80 条自动化评测集**只能手动跑**，跑失败也无人拦截——回归可能悄悄进主干。

**做法**：`evals/run_evals.py` 加退出码门禁（任一用例失败 → `sys.exit(1)`）；新增 `.github/workflows/eval-gate.yml`，push/PR 自动跑评测。

**收益**：评测从"参考工具"变成**回归拦截**。

> **待补**：目前是"冒烟式"退出码门禁（跑完没崩），**缺少带断言的单元测试**（pytest）。评测验"没死"，单测验"没做错"——后者缺失是短板。

---

## 18. 深度复盘运营档案

**问题**
长时间复盘（"昨晚整体怎么样""最近一周销量"）此前只能靠文本摘要猜数字，**没有结构化的精确查询入口**。

**做法**：新增 `agents/ops_archive.py` 的 `get_ops_archive(hours)`，聚合 `retail_stats`/`product_sales`/`heat_report` 成结构化运营档案（总客流/总销量/最热区域/分区明细/结论）；注册为 Agent 工具并加 SYSTEM_PROMPT 规则（**明确要求"不要凭文本摘要猜数字"**）。

**收益**：深度复盘有精确数据来源，不再靠模型记忆。

---

## 19. 人脸脱敏合规开关

**问题**
人脸是《个人信息保护法》下的**最高敏信息**。系统在推帧/展示时直接显示人脸画面，**识别用于统计但展示会泄露身份**。

**做法**：`cv_engine/face_emotion.py` 加 `mask_faces(frame, faces)`（对人脸区域做高斯模糊）；`config/settings.py` 加 `SANITIZE_FACES` 开关（`=1` 开启）；`api/routes/stream.py` 在表情推帧处对标注后的画面打码。

**收益**：合规可开关——识别用于统计，展示不泄露身份。

---

# 六、体验与维护性

## 20. SSE 流式回答（小程序端）

**做法**：问答页改用 `wx.request({ enableChunked: true })` + `task.onChunkReceived` 解析 SSE（按 `\n\n` 分帧取 `data: `，`[DONE]` 结束），逐 token 打字机渲染。

**与网页端的关系**：**同一个 SSE 接口**（`POST /api/queries/stream`）——浏览器用 `fetch + ReadableStream`，小程序用 `enableChunked`，**一套服务端两端共用**。

---

## 21. 本机摄像头低延迟采集

**问题**：本机摄像头采集延迟高、画面卡（串行发送 + 高分辨率 + 高质量 JPEG）。

**做法**（`App.vue`）：输出降到 320×240、帧间隔 66ms（约 15fps）、**去掉锁步串行**（原 `if(sending)` 导致卡顿）、JPEG 质量 0.4。**注意**：改造时曾误删 `vw/vh` 定义导致 `vw is not defined`，已修复。

**收益**：采集更流畅；带宽占用显著下降（对通过内网穿透/公网访问的场景尤其重要）。

> **重要认知**：局域网 HTTP 下手机浏览器**无法调用 `getUserMedia`**（非安全上下文）——"本机摄像头"必须走 https 或 localhost。这不是网络问题，是浏览器安全策略。

---

## 22. 移除原生 JS 前端，Vue 唯一化

**问题**：项目同时维护**两套前端**（原生 JS 版 `frontend/` + Vue 版 `frontend-vue/`），且有"Vue 加载失败自动回退原生版"的双路逻辑——**同一功能要改两处**。

**做法**：删除 `frontend/`（7 个文件、约 **3400 行**）；`api/main.py` 移除原生版支持（`NATIVE_FRONTEND_DIR`/`_serve_native()`/`?vue=0`），静态挂载固定到 Vue dist；Vue 缺失时显示"前端未构建"提示，Vue 加载失败改为**提示刷新**而非回退。

**收益**：单前端，维护成本减半；消除两套行为不一致的隐患。

---

## 23. 配置外置与环境变量清单

**问题**
敏感配置（DeepSeek Key、MySQL 密码、百度 AK/SK）散落在环境变量与代码里；启动要手动敲多个变量，**容易漏配**（如漏 `mysql_root` 会静默回退 SQLite）。

**做法**

- `README.md` 第 3 节重写为**完整的"必选 + 可选"环境变量清单**（含用途、默认值、示例）
- 新增 `.env.example` 模板（**只含占位符，可安全提交**）
- `.gitignore` 补 `.env` / `.env.*` / 放行 `!.env.example`

**验证**：`git check-ignore` 确认 `.env` 被忽略、`.env.example` 不被忽略。

**收益**：别人 clone 后知道要配哪些变量；**堵住"未来建 `.env` 却忘了忽略它"的泄露窗口**。

---

## 24. `.gitignore` 补全（防误提交）

**问题**
根目录混着**求职材料与临时产物**，`.gitignore` 只防了部分（如 `ScreenShot_*.png`、`项目经历_STAR.md`），漏了：

| 类别 | 内容 |
|---|---|
| 求职材料 | `agent.md`（实为 17002 行 PDF 提取的面试题库）、`Agent*.pdf` |
| PDF 提取中间产物 | `.pdf_text_raw/clean/filtered.txt` |
| pip 本地安装残留 | `.pip_tmp/`（9.4 MB）、`.pylibs/` |
| 测试残留 | `__wtest/`（**861.6 MB**） |
| 生成物/杂图 | `业务知识库_*.pdf`、`微信图片_*.jpg` |

**风险**：一旦 `git add .` 提交，**面试官会在仓库根目录看到"Agent工程师.pdf"和微信图片**。

**做法**：`.gitignore` 补全上述类别，并**逐个验证**：7 个垃圾文件 + 3 个垃圾目录确认被忽略，同时 5 份真实项目文档（`亮点二_*.md`、`并发压测报告_*.md`、`模板路由劫持防御_*.md`、`gen_kb_pdf.py`、`README.md`）确认**未被误伤**。

另：物理清理 `.pip_tmp/`（9.4 MB）+ `__wtest/`（861.6 MB），**释放约 871 MB**；3 个 `BACKUP_*` 文件先隔离保全再删除。

> **踩坑记录**：这些目录的 ACL 里有**沙箱受限令牌的 capability SID（`S-1-4-...`）**，导致当前令牌连枚举都被拒（`icacls` 报 Access denied）。解法：`takeown` 取回属主 → `icacls` 授权 → 删除。**这也是此前 `git status` 一直刷 `Permission denied` 警告的根因。**

**收益**：仓库观感干净；不会再误提交求职材料。

---

# 七、登录安全加固

## 25. 登录安全加固（6 项修复）

**背景**：鉴权上线后做了一次安全排查（用真实请求逐项验证，不靠推断）。
先确认了一个常见担心的**不成立**：**前端拿不到账号密码**——登录响应、`/auth/me`、
`/auth/users` 均不含明文密码或 `password_hash`（`_row_to_user` 根本不返回该字段）。
但排查出其它真实问题，按严重度逐个修复并逐项验证。

### 25.1 登录限流 + 失败审计（🔴 最高危）

**问题（实测）**
```
连续 12 次错误密码 → [401 × 12]   全放行，无任何拦截
单次失败尝试耗时 → 16 ms
```
**无限流 + 口令仅 6 位** → 单连接约 60 次/秒，1 万条常见弱口令约 3 分钟跑完；
且**无失败审计**，被爆破也不会察觉。

**做法**
- 失败计数存服务端（SQLite `auth_login_attempt`），重启不丢、跨进程一致
- 主维度 **「用户名 + IP」**：攻击者无法用狂试某账号的方式，把该账号的**正常用户**（不同 IP）一起锁在门外（避免"锁定即拒绝服务"）
- 另设 **IP 维度**宽松上限（用户名阈值 × 5），识别换用户名的撞库式试探
- 达到阈值 → **429 + `Retry-After`**；重复触发锁定时长按倍数递增（300→600→900…，上限 3600s）
- 登录成功即清除计数；`[AuthAudit]` 输出失败/锁定/成功日志（含用户名与 IP）

**验证**
| 断言 |
|---|
| 前 N-1 次为 401，第 N 次起 **429** 并持续拦截 |
| 429 带 `Retry-After` 正整数与可读提示 |
| **其他 IP 的正确密码仍可登录**（不被同名账号的锁定拖累） |
| 同一 IP 换用户名试探 → 最终被 IP 维度拦下 |
| 成功登录后计数清零（再次失败仍为 401 而非 429） |
| 锁定期满自动解锁（可正常登录） |
| 锁定时长递增且有上限（实测 299→599→899s） |
| 审计日志含 `[AuthAudit]` 标记、用户名、IP、"已锁定 Ns" |
| **回归**：正常登录 200、仍下发 Cookie、仍返回 token 与权限、受保护接口可用 |

### 25.2 Cookie `Secure` 标志（🟠 中危）

**问题**：`Set-Cookie: retail_sid=…; HttpOnly; Max-Age=43200; Path=/; SameSite=lax`
—— **缺 `Secure`**。走 https（花生壳）时若存在 http 降级 / 中间人，会话 Cookie 可能明文截获。

**做法**：`cookie_secure(request)` 判定 —— `AUTH_COOKIE_SECURE` 显式 1/0 则强制；
否则自动：请求是 https → 加；否则看 **`X-Forwarded-Proto`**（反向代理 / 内网穿透下外部是 https、应用侧看到 http）。
登出清 Cookie 时带**一致的**标志，确保各浏览器都能清干净。

**验证**：http 无 Secure（本地调试不受影响）｜https 有 Secure｜代理头 `https`→有、`http`→无、多值取第一段｜
`AUTH_COOKIE_SECURE=1/0` 强制生效｜登出清 Cookie 标志一致且 `max-age=0`。

### 25.3 CORS 收紧（🟠 中危，潜伏）

**问题（实测）**
```
Origin: https://evil.example.com
  → Access-Control-Allow-Origin: https://evil.example.com   ← 反射攻击者域名
  → Access-Control-Allow-Credentials: true                  ← 且允许携带凭据
```
根因 `allow_origins=["*"] + allow_credentials=True`。

**诚实说明**：**当前被 `SameSite=lax` 挡住**（跨站 fetch 不带 Lax Cookie，攻击者拿到 401），**尚不可直接利用**；
但一旦为跨站嵌入改成 `SameSite=None`，立刻变成"任意站点可复用登录态"。

**做法**：**默认不注册 CORS**（前端与后端同源，本就不需要）；确需跨域时用
`AUTH_CORS_ORIGINS` 显式白名单，只放行白名单来源。

**验证**：默认任意 Origin 都拿不到 ACAO 头（含 localhost）｜预检 OPTIONS 不放行｜
同源登录/受保护接口/WS 均正常｜显式白名单只放行白名单内来源、拒白名单外｜**回归**鉴权与限流未受影响。

### 25.4 公开端点暴露面收紧（🟡 中低）

**问题**：`/docs`、`/redoc`、`/openapi.json` 匿名可读（**完整接口清单 + 参数**）；
`/api/health` 匿名暴露 GPU 可用性、设备、运行时长（指纹信息）。

**做法**
- 文档默认**需登录**（登录用户仍可正常访问，同源 Cookie 自动携带）；`AUTH_PUBLIC_DOCS=1` 可恢复公开
- `/api/health` 匿名只返回 `{"status":"ok"}`（监控探活够用），登录后才给完整信息
- 中间件在**公开路径也尽力解析登录态**（best-effort），供这类端点按登录与否返回不同粒度

**⚠️ 过程中发现并修复一个真 bug**：初版改动**完全没生效** ——
`needs_auth()` 原本只要求 `/api/*` 前缀需登录，而 `/docs` **不在该前缀下**，
所以原来的文档白名单是**空操作**、文档一直就是公开的。已改为显式对文档前缀返回 `not AUTH_PUBLIC_DOCS`。

**验证**：匿名访问三个文档端点均 401、登录后 200 且返回接口清单｜
匿名 health 仅 `status`、不含 gpu/device/uptime，登录后完整｜`AUTH_PUBLIC_DOCS=1` 恢复公开｜
**回归**：登录/登出/前端回退上报仍公开，业务接口仍需登录，SPA 入口与静态资源仍放行。

### 25.5 口令策略（🟡 中低）

**问题**：仅要求 **6 位**、无任何强度约束 —— 与"无限流"叠加是爆破可利用的前提。

**做法**：`≥8 位`（可配 `AUTH_MIN_PASSWORD_LEN`）+ **弱口令黑名单**（含与本产品相关的易猜口令）
+ **禁止纯数字** + **禁止与用户名相同**。
**不强制大小写/符号组合**——OWASP 更推荐"足够长度 + 黑名单"，强制组合反而促使用户产出 `Password1!` 这类可预测口令。

同时修一个连带风险：`bootstrap_root()` 在 `AUTH_ROOT_PASSWORD` 不合规时**回退为随机强口令并告警**——
否则引导会抛错、root 建不出来，**等于把所有人锁在系统外**。

**验证**：7 位/6 位被拒、8 位通过｜7 个常见弱口令被拒｜纯数字被拒｜与用户名相同被拒（大小写不敏感）｜
4 个强口令不误伤（含中文）｜改密码路径同样受约束且原密码未被改坏｜
**历史短口令账号仍可登录**（策略只约束"设置"、不锁老用户）｜
**弱初始口令下仍能创建 root**（回退随机强口令）｜合规初始口令被采用｜阈值可配。

### 25.6 口令哈希核查：确认是慢哈希 + 随机盐（无需更换）

**核查结论：当前已是慢哈希（PBKDF2-HMAC-SHA256）+ 每用户 128 位随机盐，本就符合要求。**

**实测证据**
```
存储格式: pbkdf2_sha256$600000$<32位hex盐>$<哈希>
PBKDF2-HMAC-SHA256 单次耗时: 64.0 ms
裸 SHA-256 单次耗时       : 0.00045 ms
裸 MD5     单次耗时       : 0.00048 ms
慢于裸 SHA-256 约 143,447 倍   ← 证明"故意慢"，是 KDF 而非快哈希
```

| 判定项 | 结果 |
|---|---|
| 算法类型 | **PBKDF2-HMAC-SHA256**（慢哈希 / KDF，专为存口令设计） |
| 是否快哈希 | ❌ 不是 MD5/裸 SHA；比裸 SHA-256 慢 **14 万倍** |
| 随机盐 | ✅ 每用户 16 字节（128 位）`secrets.token_bytes(16)` |
| 同口令不同用户 | ✅ 哈希不同、盐不同（实测 alice/bob 同口令 → 盐完全无关） |
| 盐随机性 | ✅ 30 次计算盐互不相同，字节分布不集中 |
| 盐随哈希存储 | ✅ 格式含盐，无需额外字段即可校验 |
| 比较方式 | ✅ `secrets.compare_digest`（常量时间，防时序侧信道） |
| 接口不泄露 | ✅ `get_user`/`list_users` 均不含 `password_hash` |

**本轮做的改进**：迭代轮数从硬编码 **12 万 → 60 万**（OWASP 对 PBKDF2-HMAC-SHA256 的建议量级），
并接入配置 `AUTH_PBKDF2_ITERATIONS`；单次哈希 **64 ms**，登录体验无感。

**向后兼容已实测**：迭代轮数从**存储串**读取，因此**升级不失效旧库**——
手工构造的 12 万轮历史哈希仍能校验通过、错误口令仍被拒。

> **经验**：这属于"担心的方向错了"——真正该改的不是哈希算法（本就是慢哈希），
> 而是**限流**与**口令策略**。安全排查要逐项实测，不能凭印象换算法。

---

## 26. `.env` 加载修复（文档与实现不符）

**问题（自查发现）**
第 23 项写了「可用 `.env` 文件配置」，并提供了 `.env.example` 模板；
但**全项目没有任何 `.env` 加载逻辑**——37 处配置全是 `os.environ.get(...)` 直读进程环境变量，
既没有 `python-dotenv`，也没有自写的解析。**照模板建 `.env` 填了值，根本不会生效。**

**为什么危险**：这类"文档承诺了、实现没做"的问题最难被发现——用户按文档操作、
配置却静默无效（例如 `AUTH_ROOT_PASSWORD` 设了却不生效、`mysql_root` 设了却回退 SQLite）。

**做法**
在 `config/settings.py` 顶部加**零依赖**的极简 `.env` 加载（约 20 行）：
- 支持 `KEY=VALUE`、`#` 注释、空行、值两侧单/双引号、`export KEY=VALUE`
- **真实环境变量优先**（不覆盖），便于临时用 `$env:` 覆盖 `.env`
- **必须放在所有 `os.environ.get()` 之前**（模块顶部、`PROJECT_ROOT` 定义之后）
- `.env` 格式错误只告警不阻断启动

同时补齐 `.env.example` 中缺失的鉴权配置项（初始 root 密码、限流、口令策略、
Cookie Secure、跨域白名单、文档开关），并写明各变量的**生效时机与约束**。

**验证**
| 断言 |
|---|
| 无 `.env` 时用默认值（不报错） |
| 有 `.env` 时**配置真的生效**（修复前此处失效）——用户名/路径/数字均读到 |
| 值两侧引号被正确剥离 |
| **真实环境变量覆盖 `.env`**（`$env:` 临时覆盖可用），未覆盖项仍来自 `.env` |
| `export KEY=VALUE` 写法被正确解析 |
| 坏行（非 KEY=VALUE、空 key）被跳过且不抛异常 |
| `.env` 被 gitignore 忽略、`.env.example` 不被忽略 |

> **经验**：修完功能要**回头核对文档承诺**是否真的成立。
> 这次是"按文档操作会静默失效"的典型——比代码 bug 更隐蔽。

---

# 八、令牌不进 URL（一次性票据 + 服务端收紧）

## 27. 令牌不进 URL：一次性票据（B）+ 服务端收紧（C）

**问题（先做了暴露面核查，结论比预想窄）**
会话令牌此前可用于 URL query（`?token=`），会落进 **uvicorn 访问日志 / 反向代理日志 /
内网穿透日志**（CWE-598）。但核查发现暴露面**只在两处**：

| 客户端 | 是否把令牌放进 URL |
|---|---|
| **浏览器（Vue）** | ❌ **完全没有** —— 全部走 HttpOnly Cookie（Cookie 本身就是请求头，不进日志） |
| **微信小程序** | ⚠️ 2 处：WS（`wsUrl()`）+ TTS 音频 URL |

**根因**：浏览器 WebSocket API **不支持自定义请求头**（靠同源 Cookie 自动携带）；
小程序**没有 Cookie jar**，必须显式传凭据，而 `wx.createInnerAudioContext().src`
（音频）无法设置请求头 —— 只剩 URL 一条路。

> 顺带修正一处**我自己写错的注释**：小程序代码里曾写"小程序 WebSocket 不能自定义请求头"，
> 实际 `wx.connectSocket` **支持 `header` 参数**。但音频 `src` 确实不支持，
> 且 WS 自定义头在部分平台/基础库上有行为差异，**不能作为唯一依赖**。

**做法（B + C）**

**B — 一次性短时效票据**（给"只能用 URL"的场景一个安全通道）
```
POST /api/auth/ws-ticket          ← 用 Cookie/Bearer 正常鉴权（不经 URL，安全）
   → ticket（默认 60s 有效、**用后即焚**）
ws://host/api/ws/stream?ticket=xxx
http://host/api/tts?text=hi&ticket=xxx
```
票据存服务端（SQLite `auth_ticket`），校验时**先删除再判断**（防重放）；
账号被停用则票据立即失效；票据**不携带会话令牌**（`token=None` + `via=ticket`）。

**C — 服务端不再接受 URL 里的主会话令牌**
`?token=` 默认**拒绝**（可 `AUTH_ALLOW_QUERY_TOKEN=1` 临时兼容老客户端），
并打印可操作告警（**不打印令牌本身**）。凭据解析优先级：
`Cookie / Bearer` → `?ticket=` → `?token=`（默认拒）。

**为什么选 B 而不是"让客户端改用请求头"**：改用请求头依赖"客户端能设头"这个前提，
而**音频 `src` 物理上做不到**、**小程序 WS 的头有平台不确定性**（一旦某版本忽略该头，
WS 直接连不上，比"日志里有令牌"更严重）。B 不依赖任何平台特性，且**泄露也无效**。

**验证**

| 组 | 关键断言 |
|---|---|
| 签发 | 未登录 → 401；已登录 → 200 且返回 ticket + ttl_seconds；票据与主令牌不同 |
| HTTP 使用 | 无凭据 401 → 带 `?ticket=` **200** |
| **一次性** | 同一票据二次使用 → **401**（用后即焚） |
| **WebSocket** | 带 `?ticket=` 可正常通信；**重放被拒** |
| 过期 | 票据过期 → 401 |
| **C 收紧** | `?token=` → **401** 并给出可操作提示；**同一令牌走 Bearer 头仍 200**（只是禁止放 URL） |
| 票据≠会话 | 票据解析出的用户**不含会话令牌**；账号停用后票据失效 |
| 兼容开关 | `AUTH_ALLOW_QUERY_TOKEN=1` 时恢复旧行为 |
| **浏览器零影响** | Cookie 登录/受保护接口/SSE/WS 全通；源码中**无任何 `?token=` 构造** |
| **回归** | 角色隔离（platform → 403）与登录限流（第 N 次 429）仍生效 |

**小程序端验证（16 项断言，Node 加载真实模块）**
`wsUrl`/`ticketUrl` **不再包含会话令牌**（核心）｜正确附带 `?ticket=`（含已有 query 用 `&`）｜
https→wss｜不出现 `/api/api`｜无 ticket 时**不带任何凭据**（不退回用会话令牌）｜
`getTicket` 走 `POST /auth/ws-ticket` 且带 Bearer 头｜401 仍清登录态并回登录页｜
原业务接口未破坏。

> **过程中又修掉一个真 bug**：兼容开关 `AUTH_ALLOW_QUERY_TOKEN=1` 初版是**空操作**
> （只跳过了告警、没真的接受令牌）。是测试断言把它暴露出来的。

**顺带修复**：小程序每次连接（**含重连**）都会重新签发票据——票据是一次性的，
重连若复用旧票据会直接失败。

**⚠️ 待真机验证**：小程序端改动我只能用 Node 验证逻辑，**无法运行微信开发者工具**；
WS/音频的实际连通性需要你在开发者工具/真机上点一次确认。

---

# 九、数据来源显式化

## 28. 销量来源改为显式 `source` 字段（不再靠前缀猜）

**问题（第 6 项留下的设计缺陷，在清理测试数据时暴露）**
第 6 项用「`period_key` 是否以 `demo` 开头」来判定销量数据来源。这在实践中被证明**不可靠**：

验证阶段为测试导入链路，用**真实整点格式**（`2026091313` 等）写入了 4 个时段的销量，
结果系统把它们判为 **`pos`（真实数据）**——因为它们不带 `demo` 前缀。
**格式区分不了「真实业务数据」和「用真实格式写入的测试数据」。**

**同类对照**：项目里 `track_visit_paths` 表**早已有 `source` 字段**（`video` / `simulated`），
说明这个约定本来就有——只是 `product_sales` 没跟上。

**做法：加显式 `source` 字段（对齐既有约定）**

| 值 | 含义 |
|---|---|
| `pos` | 真实接入（POS / 人工录入），默认值 |
| `simulated` | 演示数据（`simulate_demo_sales` / `seed_traffic_demo` 显式标记） |
| `test` | **验证期写入的测试数据**（真实格式 period_key，但非真实业务数据） |

- **建表**：`product_sales` 新增 `source VARCHAR(16) NOT NULL DEFAULT 'pos'` + 索引
- **迁移**：仿照已有的 `period_key` 迁移写法（查 `information_schema` 判断列是否存在），
  加列后**自动回填**：`period_key LIKE 'demo%'` 的一律标为 `simulated`
- **写入**：`save_product_sales(..., source="pos")` —— `source` **加在参数末尾**，位置参数调用不受影响（向后兼容已实测）
- **判定**：`compare_hotness_vs_sales` 优先用**显式字段**（`get_sales_sources()`）；
  旧库缺该字段时自动退回前缀推测（`try/except` 兜底，不会崩）
- **提示**：`source_caveat()` 新增 `test` 文案——"销量为**验证测试数据**（非真实业务数据），
  不可作为经营决策依据"
- 两个演示生成器改为**显式**传 `source="simulated"`（不再指望前缀被猜对）

**存量数据标注（只改 source，数值一行未动）**

| period_key | 标注 | 行数 | 说明 |
|---|---|---|---|
| `demo` / `demo_traffic_*` | `simulated` | 6 | 演示数据（含迁移自动回填） |
| `2026091310/11/13/14` | `test` | 7 | 验证期写入的测试数据 |

**验证**

| 断言 |
|---|
| `source` 列已存在（迁移生效） |
| 演示数据**全部**标为 `simulated`（异常 0 行） |
| 验证测试数据**全部**标为 `test`（7 行） |
| **总行数未变**（13 行 → 13 行，**没有删除任何数据**） |
| 端到端：`period_key=2026091313` 归因返回 `sales_source=test` + "测试数据"提示 |
| 端到端：`period_key=demo` 仍返回 `sales_source=simulated` |
| `save_product_sales` 签名 `source` 在末尾，前 6 个位置参数顺序不变（向后兼容） |
| `classify_sources` 聚合正确：`[pos,pos]→pos`、`[test]→test`、`[pos,simulated]→mixed` |
| 全应用可导入 |

**收益**：**来源不再靠猜**——任何数据（无论 period_key 什么格式）都能被如实标注；
演示数据与测试数据都不会再冒充真实经营数据。

> **设计教训**：用**命名约定推断语义**是脆弱的（数据一旦不按约定命名就失效）；
> **该显式记录的元数据就要落成字段**。这也是项目里 `track_visit_paths.source` 早就做对的事。

> **待办**：`retail_stats`（视频热度）目前仍只有 `period_key`，演示数据用 `demo_traffic_*` 前缀隔离，
> 尚未加 `source` 字段。当前不影响使用（前缀未与真实整点冲突），如需彻底一致可后续补上。

---

# 十、容器化交付**与运行时可靠性加固**

## 29. 容器化（Dockerfile + Docker Compose）

### 问题

项目此前只能"本地直跑"，交付体验差：

| 问题 | 后果 |
|---|---|
| 要手装 Python 3.13 + 一堆系统库（OpenCV 依赖 `libGL` / `libglib2.0` 等） | clone 后跑不起来是常态，"在我机器上是好的" |
| `frontend-vue/dist` 被 `.gitignore` 忽略 | 别人 clone 后**没有前端产物**，必须自己装 Node 再构建 |
| MySQL / Qdrant / Redis 是手工 `docker run` 起的 | 启动参数散落在命令历史里，**无法复现**，换台机器就抓瞎 |
| README 无容器化途径 | 面试官/使用者没有"一条命令跑起来"的入口 |

### 做法

**① 多阶段 Dockerfile（前端在镜像内构建）**

```dockerfile
FROM node:22-alpine AS frontend      # 阶段 1：npm ci && npm run build
FROM python:3.13-slim AS runtime     # 阶段 2：装依赖 + COPY 源码 + COPY --from=frontend dist
```

`frontend-vue/dist` 不入库，所以让 `docker build` 自己构建它——**使用者无需在宿主机装 Node**，一步到位。

**② torch/torchvision 走 CPU 源**

```dockerfile
RUN pip install --index-url https://download.pytorch.org/whl/cpu torch torchvision \
 && pip install --index-url https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
```

默认 PyPI 源在 Linux 上装的是 **CUDA 版**，镜像会大 2~3 倍；而本项目的容器只跑
"业务 API + Agent + 数据侧"，CV 推理与摄像头采集本就该留在边缘。CPU 版装完后，
`requirements.txt` 里的 `torch>=2.0.0` 已被满足，pip **不会**再拉 CUDA 版
（`2.14.0+cpu` 按 PEP 440 可满足 `>=2.0.0`）。

**③ 模型与数据用挂载，不打进镜像**

`*.pt` / `*.pth` / `all_models/` / KWS 模型以**只读**卷挂载，`data/`（含鉴权库
`auth.db`、销量投递目录）为可写挂载。镜像只装代码与依赖。

**④ compose 编排 + healthcheck 门控**

`backend` / `mysql` / `qdrant`（+ `redis` 可选）。`mysql` 配 `mysqladmin ping`
健康检查，`backend` 用 `depends_on: condition: service_healthy` ——
**确保 MySQL 真就绪后才起后端**，否则首启会因连不上库而降级。

**⑤ 只发布 backend 的 8000 端口**

MySQL/Qdrant/Redis 仅在 compose 网络内被 backend 用**服务名**访问，不暴露到宿主机：
既避免端口冲突，也少一个攻击面。（需要 GUI 客户端连库时取消注释即可，已限 `127.0.0.1`。）

### 验证（全部为真实执行，非静态审查）

**镜像端到端 12 项全 PASS**（独立容器，连宿主机现有 MySQL/Qdrant，用独立 data 目录）：

| 验证项 | 结果 |
|---|---|
| 容器启动无 Traceback | PASS |
| `/api/health` 匿名探活 | PASS `{"status":"ok"}` |
| 前端静态资源（多阶段构建生效） | PASS `/app/frontend-vue/dist` |
| 首页 | PASS HTTP 200 |
| 未登录访问业务接口 | PASS HTTP 401 |
| root 自动创建（读日志密码）+ 登录 + Cookie | PASS |
| 数据可信度门禁（容器内无摄像头） | PASS `"reliable":false` |
| torch / ultralytics / opencv / langgraph | PASS `2.14.0+cpu` / `8.4.153` / `5.0.0` / `cuda False` |
| 镜像体积 | 2.99GB |

**编排端到端**（用一次性卷 + 独立项目名 `retailtest`，全程不碰用户正在跑的容器）：

- 启动顺序门控：`mysql-1 Waiting → mysql-1 Healthy → backend-1 Starting` ✔
- 服务名 DNS 解析：容器内 `mysql → 172.19.0.3`、`qdrant → 172.19.0.2` ✔
- 数据库真正建成：`retail_assistant` 库 **13 张表** ✔
- 登录/首次引导：PASS ✔
- 拆编排后复查用户环境：原容器仍 Up、`product_sales` 仍 13 行、`retail_stats` 仍 1473 行 ✔

### 实测才发现的 4 个问题

容器化里最有价值的部分不是"写了个 Dockerfile"，而是**实测暴露的这几个坑**。

#### 29.1 🔴 `.dockerignore` 漏了 `.env` → 密钥会被烤进镜像

Dockerfile 有 `COPY . .`，会把它能看到的一切打进镜像层。而 `.gitignore` 第 82 行
早就忽略了 `.env`，`.dockerignore` **却没有**——compose 注释里还写着
`cp .env.example .env`，等于在引导用户踩坑。

危害：`.env` 里是 DeepSeek API Key 与 MySQL 口令。一旦打进镜像，
任何人 `docker run --rm -it <镜像> cat /app/.env` 就能拿到，
而且**该层永久留在镜像历史里**（后续删除文件也仍可挖出）。

**容易被"看起来没事"骗过**：当时 `.env` 并不存在，所以随便一看镜像里确实没有 `.env`
——但那是**因为文件不存在，不是因为规则生效**，证明不了任何事。

严格验证（关键）：造一个带哨兵串的假 `.env`，真跑一次构建，在容器里判定文件是否存在：

```dockerfile
FROM python:3.13-slim
COPY . /ctx
RUN if [ -f /ctx/.env ]; then echo '>>> LEAK_DETECTED'; else echo '>>> CLEAN_NO_LEAK'; fi; \
    if [ -f /ctx/.env.example ]; then echo '>>> EXAMPLE_KEPT'; else echo '>>> EXAMPLE_MISSING'; fi
```

结果：`CLEAN_NO_LEAK` + `EXAMPLE_KEPT`（修复后）。

> ⚠ 本轮**第一次测出来是 `LEAK_DETECTED`，但那是测试脚本自身的 bug**：
> 我用 `grep -r LEAKSENTINEL` 判定，而哨兵串就写在 `_leaktest.Dockerfile` 的
> `RUN` 命令里，该文件本身也被 `COPY .` 拷进了 `/ctx`，于是 grep 匹配到了
> **Dockerfile 自己**。改成 `test -f /ctx/.env` 后才是有效判定。
> **教训：判定泄露要用"文件是否存在"，不要用"内容里有没有某个串"**——
> 后者会被测试脚本自身污染。

**认知**：`.gitignore` 与 `.dockerignore` 是**两套完全独立的机制**，
加了前者**完全不防**后者。凡是 `COPY . .` 的项目，两份清单要一起维护。

#### 29.2 🟠 `ffmpeg` 是纯累赘（但"凭 grep 就删"是危险的，所以做了自证）

初次写的 Dockerfile 装了 `ffmpeg`。它链式拉入 **140+ 个编解码依赖包**
（`libflite1` 12.8MB、`libcodec2` 8.2MB、`libllvm19` 26MB…），
系统层从 ~15 个包膨胀到 ~160 个，apt 阶段实测多花十几分钟、镜像大约多 200MB。

删掉它的依据（两条独立证据）：

1. 全仓库 grep `ffmpeg|ffprobe|pydub|librosa|soundfile` —— **只在 Dockerfile 命中**（即我自己写的）
2. 全仓库 grep `subprocess|os.system|Popen|shutil.which` —— **项目代码零命中**
   （唯二命中在我的临时验证脚本里）→ **不存在"调用外部命令"的路径**

因此：视频解码由 `opencv-python` wheel **内置的 ffmpeg** 完成，音频由 `sherpa-onnx`
的 Python 绑定直接读 wav。

**但"删掉后功能还在吗"必须自证**，否则就是盲删：

| 验证 | 结果 |
|---|---|
| 容器内 `command -v ffmpeg` | `NOT_FOUND`（确认已移除） |
| 解码真实 mp4（`mmpose/demo/resources/demo.mp4`） | `decoded_frames 5 size (960, 540)` |
| 读取真实 wav | `wav_ok 1 16000 89784` |

> 方法论：**"依赖没人用"是删除的必要条件，不是充分条件**——删完必须证明
> 功能仍然工作。"grep 不到"只能说明没有显式调用，证明不了库没有隐式依赖它。

#### 29.3 🔴 compose 没把 `mysql_root` 传给 backend → 静默回退 SQLite

**最危险的一类问题：不报错，但功能全废。**

现象（compose 起真实编排后看日志）：

```
[WARN] 未检测到 mysql_root，当前回退到 SQLite
Access denied for user 'root'@'172.19.0.4' (using password: NO)
```

而**同时** `/api/health` 依然返回 `healthy`、首页 200、容器状态全绿——
页面照常打开，但 MySQL 里的业务数据**一条都读不到**。这种错配在演示时可能一路装到
"报表怎么全是空的"才发现。

**根因**：compose 里的 `${mysql_root}` 只是**宿主机侧的字符串替换**
（用于拼出 mysql 服务的 `MYSQL_ROOT_PASSWORD`）。变量要真正**进入某个容器**，
只能靠该服务的 `environment:` 或 `env_file:`。我原本只在 `mysql` 服务里写了
`${mysql_root}`，`backend` 的 `environment:` 里没有 → backend 拿不到密码。

**为什么独立 `docker run` 测不出来**：那次验证我用 `-e mysql_root` 显式传了，
**恰好掩盖了编排层的缺陷**。这正说明"镜像测通"≠"编排测通"。

**修复**：在 backend 的 `environment:` 里显式传，并用 `:?` 让缺失时**直接报错**
而不是悄悄降级：

```yaml
      mysql_root: "${mysql_root:?请在 .env 或 shell 中设置 mysql_root}"
      dazuoye_api: "${dazuoye_api:-}"      # 用 :- 允许为空（无 Key 时问答降级）
```

**前后对比（实测）**：

| | 日志 |
|---|---|
| 修复前 | `[WARN] 未检测到 mysql_root，当前回退到 SQLite` + `Access denied ... (using password: NO)` |
| 修复后 | **`[OK] 数据写入: MySQL Retail_assistant`** |

#### 29.4 🟡 端口冲突 + 共享卷损坏风险

宿主机 3306/6333/6379 已被既有独立容器（`mysql-main` / `qdrant` / `redis-main`）占用，
`docker compose up` 会直接端口冲突。但**更危险的是第二个原因**：

编排复用同名卷（`mysql8_volume` / `qdrant-docker`）→ 如果旧容器不停，
**同一个卷会被两个 MySQL 同时挂载 → 数据文件损坏**。复用卷的好处是数据完整保留，
但代价就是必须先停旧容器。

处理：① 改为只发布 backend 的 8000 端口（内部服务本就无需发布）；
② 在 `docker-compose.yml` 与 README 显著位置写明迁移步骤，并建议先 `mysqldump` 备份。

**安全验证方式**：用 `-p retailtest` 独立项目名 + 一次性卷（`mysql8_volume_test`）
跑真实编排，**全程不碰用户正在运行的容器与卷**；测完 `down -v`，
再复查用户环境（原容器仍 Up、`product_sales` 13 行、`retail_stats` 1473 行）——
这两步都不能省：前者证明"测的是真编排"，后者证明"没搞坏真实数据"。

#### 29.5 🟠 `core.autocrlf=true` → Dockerfile 变 CRLF 会直接构建失败

本仓库 git 配置为 **`core.autocrlf = true`**，在 Windows 上 checkout/clone 时
git 会把文本文件转成 CRLF。而 **BuildKit 不认 `\` + `<CR><LF>` 这种行连接**
（Dockerfile 里 apt 块与 pip 块各有一处）——转成 CRLF 后会被解析成两条指令。

**A/B 实测**（同样的内容，只差换行符）：

| 版本 | 结果 |
|---|---|
| LF | 构建成功，RUN 里三行都执行 |
| CRLF | `dockerfile parse error on line 3: unknown instruction: &&` |

危险之处在于**当前工作区是 LF，所以本地构建一切正常**——问题只在
"下一次 checkout"或"别人 clone"时才爆发：**本地好好的，交付出去就挂**。

**修复**：加 `.gitattributes` 钉死 Docker 相关文件为 LF，无视宿主平台设置：

```
Dockerfile           text eol=lf
*.Dockerfile         text eol=lf
.dockerignore        text eol=lf
docker-compose.yml   text eol=lf
docker-compose.*.yml text eol=lf
*.sh                 text eol=lf
```

**验证**：`git check-attr text eol -- Dockerfile` → `text: set` / `eol: lf`；
删掉文件再 `git checkout`（模拟 clone）后查字节 → 三个文件 CRLF 行数均为 **0**；
再跑一次构建 → 全部 `CACHED`、exit 0。（对照：未钉 LF 的 `README.md` 显示 `unspecified`。）

> ⚠️ **这里我先猜错了一次，值得记下来**：我原本担心 CRLF 的 `.dockerignore` 会让
> 模式带上尾部 `\r`（即 `.env\r` 匹配不到 `.env`），从而**静默失效密钥防护**。
> 实测结果：CRLF 的 `.dockerignore` **仍然 `CLEAN_NO_LEAK`**，BuildKit 对此是容错的。
> 也就是说"**推断** CRLF 有害"只对 Dockerfile 成立、对 `.dockerignore` 不成立——
> **推断不等于结论，A/B 测过才算**。若当时按推断去"修"，就会改错地方。

#### 29.6 真实环境实战迁移（从 `docker run` 切到 compose）

29.1~29.5 都是在"一次性卷"上做的验证。本节是**在真实开发环境上真正切过去**的过程。

| 步骤 | 结果 |
|---|---|
| ① 备份 MySQL | 在容器内 `mysqldump` 写文件再 `docker cp` 拷出，**26.3MB**、含 16 张表，`product_sales`/`retail_stats` 均在 |
| ② 停旧容器 | 见下方"踩到的坑" |
| ③ `docker compose up -d` | 健康门控生效：`mysql-1 Waiting → Healthy → backend-1 Starting` |
| ④ 数据核对 | `product_sales` 13 行、`retail_stats` 1473 行、Qdrant 2 个集合**全部保留**（复用同名卷） |
| ⑤ 后端读得到数据 | 容器内经服务名 `mysql` 查询，`period_keys` 与直连结果完全吻合 |
| ⑥ 功能验证 | 首页 200、未登录 401、登录 + Cookie 200、数据可信度门禁 `reliable:false` |

**为什么不用 `> backup.sql` 在 PowerShell 里重定向**：PowerShell 的 `>` 是**文本重定向**
（Windows PowerShell 默认写 UTF-16LE 还带 BOM），会把 dump 转码，
导出的文件可能**恢复不回去**。在容器内写文件再 `docker cp` 才是字节精确的。

**踩到的坑①：`restart=always` 会让旧容器回来抢卷**

旧的 `mysql-main` 策略是 **`always`** → Docker Desktop 下次重启时它会**自动复活**，
与 compose 的 mysql **同时挂载 `mysql8_volume`**，正是 29.4 说的会损坏数据文件的场景，
而且发生得极其隐蔽。正确做法是先解除自动重启再停：

```powershell
docker update --restart=no mysql-main qdrant
docker stop mysql-main qdrant
```

> **停容器 ≠ 停干净**：带 `always` 的容器会随 daemon 启动而复活。
> 做迁移或换编排时，必须一并解除重启策略。

**踩到的坑②（自己验证脚本写错，一度误判为产品 bug）**

容器内调 `get_sales_period_keys()` 返回 `[]`，一度以为"连上了库却读不到数据"。
实为**该函数默认 `hours=1`，只查最近 1 小时**（`WHERE period_end >= cutoff`），
而数据是 9-13 与 demo 期间的，自然落在窗口外。用 `hours=100000` 复测即返回全部 6 个 `period_key`。
> **调带默认时间窗的查询函数时，先看签名再下"读不到"的结论。**

**顺带修复：Ultralytics 配置目录告警**

日志出现 `user config directory '/root/.config/Ultralytics' is not writable`。
第一次只加 `ENV YOLO_CONFIG_DIR=/tmp/Ultralytics` **没解决**，反而变成
`'/tmp/Ultralytics/Ultralytics' is not writable`——因为 ultralytics 会**再把子目录名拼到该变量之后**，
且判定依据是**父目录是否存在且可写**。故须同时 `RUN mkdir -p /tmp/Ultralytics`。
修好后告警消失，`USER_CONFIG_DIR=/tmp/Ultralytics/Ultralytics` 且目录存在。

> 坑①与坑②的共同点：**日志里的告警和"空结果"都是现象，不是原因**。
> 第一次改错方向、第一次误判，都是因为直接照现象下手。

**迁移后账号说明**：真实 `data/auth.db` 此前无账号，容器首启触发首次引导，
自动创建 `root` 并**把随机口令打印在启动日志里**。
（该口令**不写入本文档**，避免随仓库泄露；遗忘时可用 `AUTH_ROOT_PASSWORD` 指定或重建鉴权库。）
建议首次登录后立即通过 `/api/auth/password` 修改。

**日常命令与回滚**

```powershell
docker compose up -d          # 启动（镜像已构建，秒级）
docker compose logs -f backend
docker compose down           # 停止（卷保留，数据不丢）
```

回滚：`docker compose down` 后 `docker start mysql-main qdrant` 即回到迁移前状态
（旧容器仍保留，仅被停止且已解除自动重启）。

#### 29.7 🔴 迁移后两个功能静默失效：compose 只传「显式声明」的变量

**这是 29.3（`mysql_root` 漏传）的同类问题再次发生**——说明"漏变量"不是一次性失误，
而是 compose 迁移的**系统性风险**。所以这次不再逐个补，而是**系统性比对**。

**做法**：用正则从整个代码库提取所有 `os.environ.get("X")` / `os.getenv("X")` /
`environ["X"]` 的变量名（共 **37 个**），逐个比对「Windows 用户级环境变量」与
「容器内环境」，求差集。

**结果（4 个遗漏 + 1 个地址错）**：

| 变量 | 影响 |
|---|---|
| `baidu_map_ak` / `baidu_map_sk` | **地图工具全部失效**（竞品 / 商圈 / 地理编码 / 距离）——`.env.example` 里标的是**必选** |
| `LANGFUSE_PUBLIC_KEY` / `LANGFUSE_SECRET_KEY` | trace 失效 |
| `LANGFUSE_HOST` | 即使前两个补上，也会**连错地址**（见下） |

**`LANGFUSE_HOST` 不能照搬宿主机值**：宿主机是 `http://localhost:3000`，
但**容器里的 localhost 是容器自己**。实测：

| 容器内访问 | 结果 |
|---|---|
| `http://localhost:3000` | **Connection refused** |
| `http://host.docker.internal:3000` | **200 OK** |

故 compose 中硬编码后者，并加 `extra_hosts: host.docker.internal:host-gateway`
（Linux 上必须显式映射，Docker Desktop 自带）。

**第二层坑：`${VAR}` 取的是「执行 compose 的那个进程的环境」，不是「用户级环境变量」**

`LANGFUSE_PUBLIC_KEY` 明明在用户级设了（len=42），却被替换成**空值**：

| 变量 | User 级 | 该进程环境 | 替换结果 |
|---|---|---|---|
| `baidu_map_ak` | len32 | **len32** | 有值 ✓ |
| `LANGFUSE_PUBLIC_KEY` | len42 | **无** | **空** ✗ |

**结论：不要把 compose 的变量替换建立在"Windows 用户环境变量"之上**——
能否被看见取决于那个进程**何时启动**。改用 `.env` 文件：compose 会自动读取它做变量替换，
`env_file` 再把它注入容器，行为确定、可复现、不依赖 shell 状态。
（`.env` 已被 `.gitignore` 与 `.dockerignore` 双重忽略，已实测确认。）

#### 29.8 🔴 langfuse 依赖未锁上界 → trace 静默失效（**代码级缺陷，非容器专属**）

容器里 `langfuse_available()` 恒为 `False`，只打印一行
`No module named 'langfuse.decorators'`，主流程照常——**又一处静默降级**。

**三重不兼容，全部指向同一个结论：SDK 必须留在 v2**

| 对象 | 版本 | 兼容性 |
|---|---|---|
| 项目代码 | 用 v2 API `from langfuse.decorators import observe` | v3 起该子模块被移除（`observe` 挪到顶层 `langfuse.observe`） |
| 本机环境 | langfuse **2.60.10** | ✓ 子模块存在 |
| Langfuse 服务端 | 镜像 `ghcr.io/langfuse/langfuse:2` | v2 |
| **容器（修复前）** | **4.15.3**（因 `langfuse>=2.0` **没锁上界**） | ✗ 子模块不存在；且上报摄取接口返回 **404** |

**这不是容器专属问题**：任何人照 `requirements.txt` 安装都会拿到 v4 →
langfuse 被静默禁用。属于**交付级缺陷**，不只是环境差异。

**修复**：`requirements.txt` 锁定 `langfuse>=2.0,<3`，并把上述原因写进注释
（否则以后有人会"顺手"把上界去掉）。

**验证（正向确认，而非"没报错"）**：

- 容器内 SDK 版本 = **2.60.10**，与本机一致
- `langfuse_available() = True`
- 容器内真发一条 trace → 从 **langfuse 服务端 API 反查**：
  `smoke | 2026-09-16T05:22:14` **已入库**；同时看到 **9-13 本地跑时的历史
  `quick_answer` trace 仍在** —— 证明迁移没有丢历史数据

> **方法论**：验证"上报成功"不能只看"没抛异常"，要**去服务端反查**。
> v4 那次只留下一行 `404`，极易被当成噪音忽略。

#### 29.9 🟢 构建提速 8.6 倍（把此前记为"未做"的优化一并做掉）

因为要改 `requirements.txt`，pip 层必然重建——正好把之前记录的"未做优化"一起做：

```dockerfile
RUN pip install --no-deps --index-url ${PYTORCH_CPU_INDEX} torch torchvision \
 && pip install --index-url ${PIP_INDEX_URL} -r requirements.txt
```

原理：`--index-url <pytorch-cpu>` 会把 torch 的**全部依赖**也导向 pytorch 索引，
而该索引在本网络实测仅 ~30KB/s（numpy 16.7MB 单独就要约 9 分钟）。

| 阶段 | 优化前 | 优化后 |
|---|---|---|
| torch / torchvision（含依赖） | 1294 秒 | **27 秒**（只下本体，3.2MB/s） |
| requirements（清华源） | 同上一步内 | **123 秒** |
| **pip 阶段合计** | **21.6 分钟** | **约 2.5 分钟（8.6×）** |

**`--no-deps` 必须自证依赖没缺**，否则就是"构建快了但装残了"：
逐项核对 torch 的依赖（numpy / pillow / sympy / networkx / mpmath / filelock /
jinja2 / fsspec / MarkupSafe / typing-extensions / setuptools）**全部存在**，
且 `torch` / `torchvision` / `ultralytics` / `cv2` / `torch.nn` 实测可用。

#### 29.10 🔴 向量语义层在容器内整体失效（硬编码 `127.0.0.1`）

**现象**：容器启动日志出现两行 fail-open 降级，主流程照常——又一处静默降级：

```
[semantic_router] Ollama 不可用，语义闸关闭（fail-open）: Connection refused
[Vector] 向量检索失败，仅用关键词召回: Connection refused
```

**根因**：`vector_memory.py:22` **硬编码**了回环地址，且**不是**环境变量：

```python
OLLAMA_EMBED_URL = "http://127.0.0.1:11434/api/embeddings"
```

容器里的 `127.0.0.1` 是**容器自己**。实测：

| 容器内访问 | 结果 |
|---|---|
| `http://localhost:11434`（Ollama） | **Connection refused** |
| `http://host.docker.internal:11434` | **200 OK** |
| `http://qdrant:6333`（Qdrant） | **200 OK**（Qdrant 本身没问题） |

**影响面比看起来大**：`agents/semantic_router.py:24` 的 `_embed` 也是从 `vector_memory`
导入的，所以**四层仲裁的向量语义闸**与**历史会话向量召回**共用这个地址——一处失效，两者全废。

**修复**：改为环境变量可覆盖（默认值不变，本机直跑向后兼容），compose 注入宿主机地址：

```python
OLLAMA_EMBED_URL = os.environ.get(
    "OLLAMA_EMBED_URL", "http://127.0.0.1:11434/api/embeddings")
```

**验证**：
- 修复后启动日志出现 `[OK] 查询历史向量索引完成: 1738 条` —— 说明容器内确实通过 Ollama
  完成了 1738 条嵌入（修复前这一步必然失败）
- 两处 fail-open 降级均消失
- `[semantic_router] 已加载 39 条负例 + 20 条正例向量（风险阈值 0.75）` —— 闸门真正加载
- **重跑 router 评测 31/31 通过**：这一点必须做——闸门之前是 fail-open 放行，
  等于少一层判断，恢复后路由结果**可能变化**，不重跑就无法排除回归

> **方法论**：容器化时凡是 `localhost` / `127.0.0.1` 都要逐个复核。
> 本项目已因此踩了三次：`mysql_root` 漏传、`LANGFUSE_HOST`、`OLLAMA_EMBED_URL`。
> 前两次是配置问题（可用环境变量解决），这次是**代码里硬编码**，只能改代码。

#### 29.11 游离容器清理 + Redis 常驻 + 统一入口（并避开 `include:` 的坑）

**背景**：`mysql-main` / `qdrant` / `redis-main` 是迁移前用 `docker run` 起的游离容器，
不受任何 compose 管理（启动参数散在命令历史里，无法复现）。目标是把环境收敛成"全部声明式"。

**先弄清一件事：`mysql-main` / `qdrant` 不能"放进 compose"，因为它们已经在里面了**

它们不是额外组件，而是**同一部件的迁移前原件**。铁证是卷完全相同：

| 游离容器 | 卷 | compose 容器 | 卷 |
|---|---|---|---|
| `qdrant` | `qdrant-docker` | `retail-assistant-qdrant-1` | `qdrant-docker` |
| `mysql-main` | `mysql8_volume` | `retail-assistant-mysql-1` | `mysql8_volume` |

把它们也写进 compose → 两个服务挂同一个卷 → **MySQL 数据文件损坏**（29.4 记录的约束）。
正确做法是**删除**（容器只是元数据；卷与镜像都保留，且数据通过 compose 完全可访问）。
删前先把旧容器的 `docker run` 参数存档备查。

**Redis 改为常驻**：去掉 `profiles: ["full"]`，并从"可选"改为"本项目的缓存层"定位；
加 `--maxmemory 256mb --maxmemory-policy allkeys-lru`（缓存层不该吃光宿主机内存）
与 `redis-cli ping` 健康检查；复用原 `redis_data` 卷（数据保留）。
接入建议与"哪些不该进 Redis"写进了 README 与 `docker-compose.yml` 注释。

#### 29.12 ⚠ `include:` 差点毁掉 Langfuse 的数据（重要教训）

为做"统一入口"，最初想用 Compose 的 `include:` 把本项目与 Langfuse 两个文件合并。
**先用 `docker compose config`（无副作用）验证了一下，结果发现问题**：

```
顶层 name: demo_3--deepseekharness        ← 本项目自己的 name: retail-assistant 被忽略
服务: backend, db, langfuse, mysql, qdrant, redis   ← Langfuse 被并入同一项目
postgres_data -> demo_3--deepseekharness_postgres_data   ← 变成了另一个卷
```

若直接 `up`，会同时发生三件坏事：

1. 生成一整套**重复容器**，与现有容器抢 8000 端口
2. Langfuse 的 postgres 卷名从 `langfuse_postgres_data` 变成
   `<父项目名>_postgres_data` → **等于换库，Langfuse 历史 trace 全部"消失"**
   （数据其实还在旧卷里，但新容器读不到，看起来就是丢了）
3. 新 Langfuse 容器与现有容器抢 3000 端口

而且 `compose.yaml` 的**默认优先级高于 `docker-compose.yml`**——这个文件只要留在目录里，
裸 `docker compose up` 就会走这条危险路径。**已立即删除**。

**正确做法**：用包装脚本 `stack.ps1` **逐个调用各自的 `-f`**，这样每个栈都保持
自己的项目名与卷名（已验证：`langfuse` → `name: langfuse`、卷 `langfuse_postgres_data`）。
脚本支持 `up / ps / stop / start / down / logs`，且实测 **`up` 是幂等的**——
重复执行全部显示 `Running`，不重建、不新增容器（容器总数保持 6 个：本项目 4 + Langfuse 2）。

> **方法论**：`include:` 是"合并"语义，不是"并列管理"语义——它会把被包含文件的
> 服务、卷、网络统统并入父项目命名空间。凡是**依赖目录名推导项目名/卷名**的 compose 文件
> （Langfuse 就是：没有显式 `name:`，卷也没有显式 `name:`），都不能被 `include`。
> 更普遍的一条：**破坏性操作用 `config` 先验证一遍**，成本几秒，收益是避免一次数据事故。

#### 29.13 🔴 镜像里曾含 27.5MB 真实业务数据（`.dockerignore` 漏 `*.sql`）

**发现**：审计时直接查镜像内容，发现 `/app/backup.sql`：

```
-rwxr-xr-x 1 root root 27548717 Sep 16 12:36 /app/backup.sql   >>> 业务数据已进镜像层
.pdf_text_clean.txt / .pdf_text_filtered.txt / .pdf_text_raw.txt（约 3MB 个人材料）
```

**根因**：`.dockerignore` 有 `*.sqlite` / `*.sqlite3`，**却没有 `*.sql`**。
而 `backup.sql` 是 29.6 迁移前用 `mysqldump` 导出的（含 `retail_stats` 全量、销量、会话等），
在之后几次构建中被 `COPY . .` 烤进了镜像层。

**为什么 `gitignore` 挡不住**：两者是**独立机制**——`.gitignore` 里确实早就加了 `*.sql`，
但那只防 git，**完全不防 Docker**。（同一个道理在 29.1 的 `.env` 上已经吃过一次。）

**危害与"其实没事"的部分**（诚实区分）：
- 危害：业务数据不该随镜像外流；且**镜像层是永久的**，事后删文件仍可从旧层挖出
- 其实没事：该镜像**从未推送**到任何仓库，所以没有实际外泄；且 dump 里**不含**
  口令哈希（哈希在 SQLite 的 `auth.db`，不在 MySQL dump 中）

**修复**：`.dockerignore` 补 `*.sql` / `backup*.sql` / `.pdf_text_*.txt` / `业务知识库_*.pdf`
（最后一项经确认：只由 `gen_kb_pdf.py` 生成，**无任何代码读取或提供下载**）。

**验证**：重建后镜像内**无 `.sql` / 无 `pdf_text` / 无 `pdf`**，项目代码完整，
体积 **3.01GB → 2.97GB**。

> **方法论**：密钥/数据类清单，`.gitignore` 与 `.dockerignore` 必须**成对维护**。
> 更主动的做法是**定期直接查镜像内容**（`docker run --rm <镜像> ls -a /app`），
> 而不是只审查 `.dockerignore` 写了什么——因为"规则看起来对"和"实际排除了"是两回事。

#### 29.14 就绪探针（破解"假健康"）+ 日志轮转 + 生产路径澄清

**背景**：第三方给出的评估里，"缺监控"这条有个真内核值得做——容器日志无轮转、
健康检查是假的。其余几条（缺 Redis / 缺 volumes / 端口冲突 / 没前端）经实测**均与现状不符**，
详见下方"对第三方评估的核对"。

**① `/api/health/ready`：把 liveness 与 readiness 分开**

原来只有一个 `/api/health`，它**不查任何依赖**、无条件返回 `{"status":"ok"}`——
而 Dockerfile 的 `HEALTHCHECK` 正绑在它上面。于是"MySQL 口令没传进容器 → 后端静默回退
SQLite → health 依然 healthy"能一路蒙过编排与看板。**liveness 探针天然发现不了这类问题。**

新增 readiness 端点，并**有意区分"致命"与"降级"**（避免探针一抖动就把服务判死）：

| 依赖 | 判定 | 理由 |
|---|---|---|
| MySQL | **→ 503** | 业务数据读写全废，服务等于不能用 |
| Qdrant / Redis | 200 但列入 `degraded` | 向量召回退化为关键词、缓存未命中，主链路不受影响 |
| CV 引擎 | 列入 `degraded` | 容器内没有摄像头属预期 |

MySQL 检查走**真实 `SELECT 1`**，而不是 `mysql_available()`——后者只看环境变量，
正是当初"假绿灯"的根源。响应只回状态与异常类型，**不回显主机名/凭据等指纹**。
`/api/health/ready` 已加入鉴权白名单（探针无法携带凭据）。

**验证（故障注入，正反两个方向都测）**：

| 端点 | MySQL 正常 | **MySQL 停止** |
|---|---|---|
| `/api/health` | 200 `{"status":"ok"}` | **200 ok**（设计如此，向后兼容） |
| `/api/health/ready` | 200 `{"status":"ok","critical":{"mysql":true}}` | **503** `{"status":"unavailable","critical":{"mysql":false},"detail":{"mysql":"OperationalError"}}` |

且**恢复 MySQL 后自动回到 200，无需重启 backend**（连接池能自愈）——这一点也实测确认。

**② 日志轮转**：四个服务统一配 `json-file` + `max-size: 50m` + `max-file: 5`。
项目有 211 处 `print()` 全走 stdout，而 Docker 默认无上限；叠加每 10 分钟一轮 LLM 汇报
与每 60 秒销量扫描，长期运行会吃满宿主机磁盘，**而磁盘满又会连带 MySQL 写入失败**。

**③ 生产路径写进 README**：明确列出 Nginx / Prometheus+Grafana / 日志聚合 / MQ
是**上线才需要**的，当前**有意不做**并说明理由——避免"为了显得完整"而引入
对单机单店场景过度的组件。

#### 29.15 对第三方评估的逐条核对（三条与现状不符）

拿到一份第三方 AI 评估，逐条实测核对后**没有照做**：

| # | 评估说法 | 裁定 | 实测依据 |
|---|---|---|---|
| 1 | 缺 Redis | **已做** | 29.11 已常驻（`maxmemory=256MB` + `allkeys-lru` + 复用 `redis_data`）。但"高频打满 MySQL"仍是无数据的推测 |
| 2 | 纯同步 API，该上 MQ | **不准确 + 过度** | 已有后台线程：`scheduled_tasks.py:91`、`sales_inbox.py:169`、`stream.py:605`。真正该修的是**处理线程异常静默死亡**（子代理实测：抛异常后仍报 `running:True`，画面永久卡死） |
| 3 | 没配 volumes，删容器就丢数据 | **❌ 不成立** | `mysql8_volume`/`qdrant-docker`/`redis_data`/`langfuse_postgres_data` **4 个卷都在**，且用显式 `name:` 固定 |
| 4 | 新旧容器会抢 3306/6333 | **❌ 已解决** | 旧容器已删除；且 compose **只发布 backend 的 8000**（`3306/tcp` 是容器内端口，无宿主映射）→ 连"改成 3307"都不需要 |
| 5 | 没有前端 | **❌ 不成立** | 首页 **200**，`/app/frontend-vue/dist/index.html` 在镜像内（多阶段构建），小程序也在 |
| 6 | 缺监控 | **部分合理（已取其内核）** | 见 29.14：只取"日志轮转 + 真实健康检查"，未上 Prometheus/Grafana/ELK |

> **方法论**：收到的技术评估**先核对再执行**。这份评估里有 3 条与现状完全相反
> （volumes / 端口 / 前端），若照做会白干甚至改坏（例如把已不冲突的端口改成 3307）。
> 判断依据要落在**实测**上，而不是评估文字的自信程度。

#### 29.16 🔴 两个路由文件在事件循环里同步直调 DB，能把全站冻住

**问题**：`api/routes/chat.py`（7 个 `async def`）与 `api/routes/emotion_camera.py`
在路由体里**直接同步调用** MySQL / Qdrant / Ollama 函数。它们是 `async def`，
所以这些同步调用跑在**事件循环线程**上——一次慢查询就会让同时刻的视频帧推送、
SSE 流、心跳全部停摆。最坏路径是 `chat.py` 的向量搜索 → `_embed` 的
`httpx.post(timeout=60)`：**一次 Ollama 慢响应 = 全站 60 秒无响应**。

**这是遗漏而非设计选择**——证据是项目**其它地方全都对**：

| 文件 | `asyncio.to_thread` 次数 |
|---|---|
| `analytics.py` | **14** |
| `master_agent.py` | 7 |
| `maps.py` / `query.py` | 4 / 4 |
| 两个 checkpointer | 8 |
| `tts.py` / `main.py` | 2 / 2 |
| **`chat.py` / `emotion_camera.py`** | **0 / 0** ← 只有这两个漏了 |

**修复**：全部改为 `await asyncio.to_thread(...)`。`delete_session` 里"删库 + 逐条删向量"
的循环整体包进一个紧函数再 `to_thread`（避免 N 次协程切换）。

**验证（同一测试的前后对比）**：把会话列表查询桩成阻塞 1 秒，并发打两次：

| | 两个并发请求总耗时 |
|---|---|
| 修复前 | **2.01s**（串行 → 事件循环被阻塞） |
| 修复后 | **1.00s**（并发 ✓） |

功能回归 8 项全 200：新建/列出/重命名/取消息/搜索/删除（含删向量）/表情汇总/摄像头状态。

> ⚠ **过程中的一个自我纠错**：我第一次跑这个测试得到 2.00s，一度以为修复无效——
> 实际是**改了宿主机代码却没重建镜像**，容器里跑的还是旧文件。
> 先用 `docker compose exec backend grep -c to_thread /app/api/routes/chat.py`
> 得到 `0` 才确认。**改完代码要先确认"容器里真的是新代码"，再下"修复无效"的结论。**

#### 29.17 🔴 处理线程静默死亡（画面永久卡死）+ ROI 并发改写

这两个是**同一条故障链**：ROI 并发改写触发异常 → 处理线程无兜底 → 静默死亡。
所以一起修、一起验证。**两条都先自己复现确认，再改。**

**① H3：处理线程异常即静默死亡**

`_processing_thread_retail` / `_processing_thread_emotion` 的循环体**原本没有任何
try/except**，而 `get_stream_status()` 只读 `_active["running"]`。

**复现（真实 `stream.py` + 桩处理器，第 30 帧抛异常）**：

```
Exception in thread retail-processing:
  File "/app/api/routes/stream.py", line 419, in _processing_thread_retail
RuntimeError: 模拟推理阶段异常（第 30 帧）

处理线程还活着吗 : False
get_stream_status: {'running': True, ..., 'current_frame': 29}
```

即：线程已死、状态仍报"运行中"、帧号停住，**异常只进服务端 stderr，客户端什么都收不到**，
必须人工重启服务。

> 第一次复现**没成功**（线程活着、只处理 3 帧）——因为 `_client_frames` 是
> `maxlen=3` 的 deque，我一次性塞 300 帧只留下 3 帧就空转了。改成持续喂帧才复现。
> **又一次"测试设置错"与"产品 bug"的区分**。

**修复**：在**线程入口**统一兜底（一处覆盖零售+表情两种模式），复用既有的
`_latest_result` 约定把错误交给 WebSocket 循环。选这个位置的额外好处是
不必改两个循环体，也不需要改动 `start_processing` 之外的调用方。

**② M5：ROI 单例被 API 线程改写 → 处理线程抛异常**

`ROIManager.zones` 被处理线程**每帧**遍历（`get_zone` 判区域、`draw_zones` 画框），
而被 API 线程改写（`PUT /api/zones` → `add_zone`/`remove_zone`），**全程无锁**。

**复现过程很值得记**（同一个 bug，两种测法结论完全不同）：

| 测试模式 | RuntimeError 次数 |
|---|---|
| 增 + 删**成对**（大小很快复原） | **0 次** ← 差点误判为"没问题" |
| 只增不删（**真实用户行为**：保存后留着） | **200/200 次全抛** |

原因是 CPython 的"字典大小变化"检查依赖 `ma_used` 是否改变——增删成对时，
字典大小在迭代器下一次取元素前就复原了，探测不到。
**所以"改了没炸"不能证明并发安全，必须用接近真实操作的时序去测。**

**修复**：`add_zone` / `remove_zone` / `update_zone_polygon` 改为 **copy-on-write**
（复制新字典再原子替换引用）。遍历方要么拿旧快照、要么拿新快照，永远不会看到
"正在被修改"的字典；引用赋值本身是原子的，无需加锁。

**验证（同一复现脚本的前后对比）**：

| | 修复前 | 修复后 |
|---|---|---|
| M5：只增不删 200 次 | **200 次 RuntimeError** | **0 次** |
| H3：`running` | `True`（撒谎） | **`False`** |
| H3：`thread_alive` | 无此字段 | `False`（新增，第二道保险） |
| H3：错误通知 | 仅服务端 stderr | `{"type":"error","message":"RuntimeError: ..."}` |

**完整链路**（前端早已实现，服务端修复直接接上）：

```
处理线程抛异常 → _guard 捕获并复位 running=False
  → _latest_result = {"type":"error", ...}
  → WS 循环新分支发 {"status":"error","message":"视频分析异常已终止：…"}
  → 前端 stream.js:263 updateStatus('warning', msg)   ← 早已有这个分支
  → 下一轮 _latest_result 为 None → 再发 stopped → 画布复位
```

用户从"只看到画面卡住、不知道发生了什么"变成"看到明确的异常提示 + 界面复位"。

#### 29.18 🔴 连接池满时无上限新建裸连接——"降级反而放大故障"

**复现（真实配置，打桩 `pymysql.connect` 计数）**：

```
真实池配置: pool_size=5 max_overflow=10 pool_timeout=30.0s
  -> 并发上限 15；第 16 个调用者要先等 30s 才降级

池满后 30 次调用 -> 新建裸连接 30 条
耗时 30.8s
```

MySQL `max_connections=151`。即：并发一高，**降级路径会无限新建连接**，
从"部分请求变慢"升级为"撞满 max_connections、全库不可用"。
**这类"降级反而放大故障"的路径比直接失败更危险**——因为它看起来是在"容错"。

**关键发现：有两层堆叠的兜底，只修一层是无效的**

`db_engine.pooled_connection()` 之下，`mysql_db.get_connection()` 还有第二层
`except Exception` 兜底。实测确认它会把池满异常吞掉、再新建裸连接：

```
通过 mysql_db.get_connection()（生产入口）调用 10 次
-> 新建裸连接 10 条
=> 只修 db_engine.py 无效，必须同时处理 mysql_db.get_connection()
```

**修复**（区分两种"拿不到池化连接"的情况，处理方式有意不同）：

| 情况 | 处理 | 理由 |
|---|---|---|
| `engine is None`（池没建起来） | **保留**裸连接降级 | 稳态降级（启动时 MySQL 不可达 / 缺驱动），连接由调用方 close()，不堆积 |
| `raw_connection()` 抛异常（**池满**） | 抛 `DBPoolBusy`，**不再**新建裸连接 | 快速失败 >> 降级放大故障 |

并把 `pool_timeout` 从**隐式 30 秒**改为显式 **5 秒**——30s 对 Web 请求太长，
池一紧张每个溢出调用者都白等 30s，把请求线程一起拖住。
`mysql_db.get_connection()` 对 `DBPoolBusy` **原样抛出**，不得被通用 `except` 吞掉。

**验证**：

| | 修复前 | 修复后 |
|---|---|---|
| 池满后 10 次调用 | 0 次抛错、**10 条裸连接** | **10 次 `DBPoolBusy`、0 条裸连接** |
| 正常路径 | — | 查询正常（`product_sales=13`） |

> **方法论**：排查"降级路径"时要**顺着调用链把每一层兜底都找出来**。
> 这个项目里同一个决策（"拿不到池化连接怎么办"）在两层各写了一遍，
> 只改上面一层，问题一点都不会变。

#### 29.19 🔴 两个用户同时开「本机摄像头」会互相串台

**先甄别清楚**：服务端摄像头/视频文件模式是"**一个源、多人观看**"，全局共享是**合理设计**。
出问题的是**本机摄像头模式**——它把每个浏览器**自己的**摄像头帧推进同一个队列，
却共享同一个进程级会话，必然互相踩。

**复现**（两个**不同账号**的真实 WS 客户端，走前端真实路径）：
- `/api/ws/stream` 控制 + 收帧（二进制：`frame_id(4B) + JPEG`）
- `/api/ws/client` 推本机摄像头帧（见 `frontend-vue/public/js/stream.js:297`）

```
阶段1（只有 A 推红帧）            A 收到 91 帧: {'RED': 91}
阶段2（B 启动并推蓝帧，A 停止推帧）
    A 收到 108 帧: {'RED': 3, 'BLUE': 105}   <- A 在看 B 的摄像头
    B 收到 114 帧: {'RED': 9, 'BLUE': 105}
```

B 一点"本机摄像头"就会**杀掉 A 的处理线程、清空 A 的帧队列**，
然后 A 继续显示 B 的画面而毫无提示。

**复现过程中的三个自我纠错**（都值得记）：

1. 帧是**二进制**（`frame_id + JPEG`），不是 JSON——我最初只解析 JSON，收到 0 帧
2. **只拦 `start_client_camera` 无效**：A 停止推帧后仍收到 105 帧 B 的画面——
   因为前端根本不用 `client_frame` 这个 JSON action，而是另开 `/api/ws/client` 推二进制帧，
   那条链路**完全没有归属校验**
3. 漏了 `global _client_camera_owner_ws` 声明 → Python 当成局部变量 →
   `UnboundLocalError` → WS handler 直接崩溃 → 测试出现"0 帧"的**假通过**
   （0 帧不等于"隔离成功"，很可能只是"根本没跑起来"）

**修复**（两层归属，因为两条连接不是同一个 websocket 实例）：

| 用途 | 键 | 为什么 |
|---|---|---|
| 启动仲裁 | `_client_camera_owner_ws`（**连接身份**） | 同一用户开两个标签页用户名相同，只比用户名会让第二个标签页抢占第一个 |
| 推帧过滤 | `_client_camera_owner`（**用户名**） | `/api/ws/client` 是另一条连接，只有"同一个登录用户"能关联两者 |

用户名从 `scope["state"]["user"]` 读（鉴权中间件已写入），
**不用** `user_from_scope()`——它会消费一次性票据。
归属在 `stop` 与 WS 断连时释放，避免该模式被永久占用。

**验证（同一脚本前后对比）**：

| | 修复前 | 修复后 |
|---|---|---|
| A 收到 B 的画面 | **105 帧** | **0 帧** |
| B 的提示 | 无（静默抢占） | `error: 已有其他用户正在使用「本机摄像头」模式…` |

**残留局限（诚实说明）**：同一用户开两个标签页时，第二个标签页的启动会被拒绝，
但它的 `/ws/client` 帧因用户名相同仍会通过过滤。彻底解法是把会话状态
（`_active` / `_run_token` / `_latest_result` / `_latest_frame_b64` / `_client_frames`）
收进 per-connection 会话对象——那是较大的重构，见待办。

#### 29.20 🔴 SQLite 降级路径的表从未创建 → 表情数据静默丢失

**机制**：`database.py` 的设计是"平时写 MySQL，MySQL 挂了自动落 SQLite"。
但 SQLite 的建表语句只在「MySQL 不可用」这个分支里执行，而 MySQL 正常启动时
`init_db()` 会**提前 `return`**——于是 SQLite 里根本没有 `emotion_record` 表。

**复现**（把 `EMOTION_DB_PATH` 指向临时库，不碰真实数据）：

```
=== 场景A：MySQL 启动时可用（正常运行情况）===
SQLite 文件是否被创建: False          <- 连文件都没建
SQLite 里的表: (无)

=== 场景B：运行期 MySQL 掉线，写入降级到 SQLite ===
[database] MySQL 批量写入失败，回退 SQLite: MySQL server has gone away
降级写入: 失败 -> OperationalError : no such table: emotion_record
```

**后果**：MySQL 一抖，这段时间的表情数据就**丢了**——外层 `except` 只打一行日志，
没有重试、没有队列、也没有告警。**新容器首次启动必然处于场景A**，所以这是必踩的坑。

**修复**：`init_db()` 里删掉那个 `return`，让 **MySQL 与 SQLite 两条路径都建表**。
建表是幂等的（`IF NOT EXISTS`）且开销极低，但降级路径从此才真正可用。

**验证**：

| | 修复前 | 修复后 |
|---|---|---|
| 场景A SQLite 表 | 文件都没创建 | `['emotion_record', 'sqlite_sequence', ...]` |
| 场景B 降级写入 | `no such table` 失败 | **成功** |

> 顺带说明：你当前 `data/shop_emotion.db` 里其实**已有** `emotion_record`（1102 行）——
> 因为之前几次排查时停过 MySQL，降级路径被执行过、顺手建了表。
> 所以你的历史数据没丢，但**全新部署会踩**，这一点由受控测试证实。

#### 29.21 🔴 MySQL 掉线时每次 DB 调用白等 8 秒——把"数据库故障"放大成"全站卡死"

**复现**（停掉 MySQL，连续调用 `mysql_db.get_connection()`）：

```
第1次 耗时 12.1s -> OperationalError
第2次 耗时  7.9s -> OperationalError
第3次 耗时  7.9s -> OperationalError
```

原因：每次都要先尝试 TCP 连接、一直等到 `connect_timeout` 才抛错。

**危害不止"慢"**：这些调用大多来自 `asyncio.to_thread` 的线程池
（29.16 那次修复把它们都放进了线程池，正是对的），而
**每个挂起请求占住一个 worker**。挂起的多了，所有异步路由都要排队——
数据库故障于是被放大成"整个 API 无响应"。

**修复：熔断（circuit breaker）**

连续失败 N 次（3 次）后进入冷却窗口（15s），窗口内**直接快速失败**，不再尝试建连；
冷却结束放一次探测，成功即复位。

另外两处配套修正：
1. `pooled_connection()` 的熔断检查**必须放在 `get_engine()` 之前**——否则每
   `_POOL_RETRY_INTERVAL`（30s）仍会先触发一次建池尝试、白等约 12s
2. 降级路径的 `connect_timeout` 从 10s 压到 3s（它是降级路径，不该让请求挂 8~10 秒）

**验证**：

| 场景 | 修复前 | 修复后 |
|---|---|---|
| 故障期间每次调用 | **~8s**（12.1 / 7.9 / 7.9 …） | 第1次 12s，**之后全部 0.00s** |
| 跨过 30s 建池周期 | 又一次 ~8s | 一次 3.9s 探测，之后 0.00s |
| MySQL 恢复 | — | 第 1 次即成功（0.15s）并**自动复位**熔断 |

> 注意第 1 次仍是 ~12s：那是"首次发现 MySQL 不可用"的必然代价（真实 TCP 超时）。
> 熔断能优化的是**后续每一次**，而这正是"放大成全站卡死"的来源。

#### 29.22 🔴 同会话并发写入全部撞成同一个 seq_no

**复现**（8 线程用 `Barrier` 同时写同一 `session_id`，真实 MySQL）：

```
并发写入 8 条，成功 8 条
seq_no 分布: [1, 1, 1, 1, 1, 1, 1, 1]
重复的 seq_no: {1: 8}
库中实际 seq_no: [1, 1, 1, 1, 1, 1, 1, 1]
期望值应为 1..8 各一次 -> False
```

比报告描述的（"只剩 2 个序号"）更严重：**8 条全部撞成 1**。
根因是 `save_query_history` 里「`SELECT MAX(seq_no)+1`」与「`INSERT`」是两条独立语句，
而池化连接是 `autocommit=True`，中间没有任何保护。

**危害不止"顺序乱"**：`api/routes/query.py:93` 用 `seq_no == 1` 判断"会话首条消息"
来决定是否用 LLM 生成标题——撞号会让它**重复触发**（多余的 LLM 调用 + 标题被覆盖）。

**修法的选择过程（先做对照实验再改代码，避免了两次错误的方向）**：

| 候选方案 | 实测结果 | 结论 |
|---|---|---|
| 单条 `INSERT ... SELECT COALESCE(MAX(seq_no),0)+1` | 并发下 **MySQL 死锁 1213**，8 线程 5~6 个失败；加 3 次重试仍有 1 个失败 | ❌ 不可取 |
| `SELECT ... FOR UPDATE` + 显式事务 | 需要对**池化连接**来回切 `autocommit`，异常路径没恢复就会污染后续复用该连接的业务 | ❌ 风险高 |
| **进程内锁串行化读-改-写** | 8 线程 → `[1,2,3,4,5,6,7,8]` | ✅ 采用 |

本项目 compose 是**单副本**部署，进程内锁即可彻底解决；
将来多进程化时需回到 DB 层方案（已记入待办）。

**验证**：

| | 修复前 | 修复后 |
|---|---|---|
| 8 线程同会话并发写入 | `[1,1,1,1,1,1,1,1]` | **`[1,2,3,4,5,6,7,8]`** |

> **方法论**：这次没有直接照报告的建议去改，而是**先写了一个对照实验脚本**
> 把候选修法在真实 MySQL 上跑一遍——结果第一个候选方案就被否决了（死锁）。
> 如果直接改代码，会引入一个"看起来修好了、并发高时反而报错"的新问题。

#### 29.23 🟡 多路常驻引擎的 `_retired` 队列无界增长（慢泄漏）

**机制**：`cleanup()` 把退役轨迹压进 `_retired`，等上层 `drain_retired()` 取走落库
（CV 层保持零 DB 依赖）。但 `cv_engine/multi_stream.py` 的多路常驻引擎
**只调 `process_frame`、从不 drain**，于是该队列随客流线性增长。

**复现**（模拟 5000 个行人依次经过）：

```
经过 5000 个行人后：
  _tracks (活跃) = 0
  _retired (退役) = 5000
  单条 TrackState 约 296 字节 -> _retired 累计约 1.4 MB
drain_retired() 后 _retired = 0      <- 说明只要有 drain 就不会涨
```

**诚实评估严重度**：这是**慢泄漏**，不是急症。
按 pickle 估算约 1.4MB / 5000 人次（实际内存会更高），单店每天可能新增 1~2MB，
一个月几十 MB。只有长跑数月的服务才会明显。

**修复**：给退役队列设上限（保留最近 1000 条）并计数丢弃量。
理由：它只是"待 drain 的暂存"，不该是无限缓冲区；而多路场景下的动线
**本来就没有被持久化**（没人 drain），所以丢弃不影响既有功能。
超过上限时打印一次提示（`retired_dropped` 可用于观测"调用方没 drain"）。

**验证**：

| | 修复前 | 修复后 |
|---|---|---|
| 5000 行人后 `_retired` | **5000**（约 1.4MB） | **1000**（约 0.3MB，有界） |
| `drain_retired()` | 正常 | 正常（不受影响） |

> 顺带发现一个**功能缺口**（非 bug）：多路摄像头模式下的动线从未落库，
> 因为没人 drain。单路 WS 路径（`api/routes/stream.py:453`）每 30 帧 drain 一次，
> 所以只有单路模式有动线数据。补多路动线持久化需要产品决策，未在本轮做。

### 收益

- 交付从"照文档装两套运行时 + 手敲 docker run"变成 **`docker compose up -d --build` 一条命令**
- 使用者**无需安装 Node**（前端在镜像内构建），也无需手装系统库
- 依赖容器从"命令历史里"变成**声明式、可复现**，且带健康检查门控
- `.dockerignore` 与 `.gitignore` 双清单到位，**密钥不会进镜像也不会进仓库**
- 少装 ffmpeg：系统层包数 ~160 → ~15
- **pip 阶段 21.6 分钟 → 2.5 分钟**（29.9），改依赖后重建不再痛苦

### 已知取舍（诚实说明）

- **镜像约 3.0GB**：主要是 `torch` CPU 版（196MB 轮子解压后更大）+ `opencv-python` 依赖的
  Mesa GL/LLVM 系统库。若要更小可换 `opencv-python-headless`（不依赖 libGL），
  但 `ultralytics` 声明依赖 `opencv-python`，混装会有冲突风险，本轮未动。
- **构建耗时**：已优化，见 29.9 —— pip 阶段从 21.6 分钟降到约 2.5 分钟
  （`torch` 用 `--no-deps` 只下本体，依赖改由清华源补齐，并已自证依赖完整）。
- **容器内无摄像头**是**有意为之**（边缘/云端分工），
  此时"数据可信度门禁"会明确报 `reliable:false`，不会把 0 当业务结论。
- **真实环境已切换完成**（29.6），旧容器保留可回滚。但要注意：
  compose 的变量替换依赖**执行 compose 时的进程环境**，
  因此**必须用 `.env`**（29.7），不能只靠 Windows 用户环境变量。

---

# 十一、按模块归档的修复记录（2026-09 起的新条目放这里）

> **记录约定（自 2026-09 起生效）**：新发现的问题不论大小，都按**模块**归档到本节，
> 统一按「**问题 → 复现 → 根因 → 修复 → 验证**」五段式记录，修复过程一并留下
> （包括走过的弯路和错误的验证方式——那些往往比结论更有价值）。
>
> 并把对应条目在**附四台账**里标记为已修，在**上方总索引**登记编号。
> 这样文档有三个入口：
>
> | 入口 | 用途 |
> |---|---|
> | 上方**编号索引**（第 1~N 项） | 按时间序浏览全部改进 |
> | **附四 台账** | 按严重度看"还有哪些没修"，带可信度标注 |
> | **本节 十一** | 按模块定位"这个模块历史上出过什么问题" |

## 模块导航（全部条目 → 模块）

> 覆盖本文档全部条目（含早期记在 一~十 部分的），便于按模块检索。

| 模块 | 涉及条目 |
|---|---|
| `cv_engine/`（视觉引擎） | 12（人脸 `faces=None`）、14（多路并行）、19（人脸脱敏）、**A1**（`detect()` 缺 return） |
| `agents/`（Agent 与业务分析） | 2（StateGraph）、4（可信度门禁）、5（同期对比）、6（销量来源）、7（销量自动同步）、15（地图缓存）、18（运营档案）、**A2**（客流 SUM 口径）、**A3**（period_key 粒度）、**A4**（问答链路门禁）、**A5**（按源判定）、**A6**（转化率口径）、**A7**（断流 0 当低谷）、**A10/A11**（告警队列封顶 + 突增基线）、**A12**（周期对比未截断到当前分钟）、**A13**（来源判定吞成 unknown）、**A15**（DB 故障当没数据）、**A20**（补货意图被热度模板劫持） |
| `api/`（接口层与鉴权） | 1（登录鉴权）、8（汇报 WS）、9/10（多端登录）、13（lifespan 顺序）、25（登录安全 6 项）、27（一次性票据）、**36**（就绪探针）、**38**（事件循环阻塞）、**39/40**（线程死亡 + ROI 并发）、**42**（串台）、**B2**（权限矩阵落地）、**B7**（安全响应头）、**B10**（口令策略上界与空白）、**C4**（`_active_ws` 登记不一致） |
| 数据层（`mysql_db` / `db_engine` / `database`） | 11（压测分位单位）、28（销量 source 字段）、**41**（连接池裸连接）、**43**（SQLite 降级表）、**44**（熔断）、**45**（seq_no 竞态） |
| 前端 Vue（= 下方 **模块 E**） | 9（登录页）、21（本地摄像头低延迟）、22（移除非原生前端）、**D1**（`voice.js` 加载顺序 + 守卫） |
| 小程序（= 下方 **模块 E**） | 10（Bearer 适配）、20（SSE 流式）、**D2**（`start_webcam` 报文不匹配） |
| 向量与记忆（`vector_memory`） | 3（Qdrant Server 模式）、**33**（硬编码 127.0.0.1） |
| 交付与运维（Docker / CI / 配置） | 16（摄像头配置持久化）、17（评测门禁）、23（配置外置）、24（`.gitignore`）、26（`.env` 加载）、**29**（容器化）、**30~32/34/35/37**（迁移与加固）、**46**（`_retired` 有界化）、**E1**（CI 密钥变量名）、**E7**（mysql healthcheck 改为真实查询） |

---

## 模块 A · `cv_engine/`（视觉引擎）

### A1. 表情识别一检测到人脸就崩溃（`detect()` 缺 `return faces`）

**问题**
`cv_engine/face_emotion.py` 的 `FaceEmotionDetector.detect()` 在循环里构建并 `faces.append(...)`，
但**函数末尾没有 `return`**，隐式返回 `None`。
无脸帧走的是提前 `return faces`（空列表）那条分支——于是形成最恶劣的组合：
**"没人的时候一切正常，一来人就崩"**。

**复现**（打桩模型，让它"检测到一张脸"）
```
检测到 1 张人脸时 detect() 返回 = None
调用方 stream.py:311 执行 for face in faces -> TypeError: 'NoneType' object is not iterable
```

**根因**
纯遗漏。**这是同类 bug 在本项目的第二次出现**——`cv_engine/video_processor.py`
此前也栽在 `faces=None` 上（见第 12 项），修的是那一处，没顺手排查同类调用点。

**修复**
补 `return faces`，并在注释里写明为什么必须显式返回、以及历史上出现过两次。

**验证**
```
检测到 1 张人脸时 detect() 返回: list [{'bbox': [10,10,120,120], 'emotion': 'angry', ...}]
调用方 for face in faces 可正常迭代 -> PASS
```

> **方法论**：修"某处返回 None 导致调用方崩溃"时，**要顺手全仓搜同类调用点**，
> 而不是只修报错的那一处。本项目两次踩同一个坑就是这个原因。

---

## 模块 B · `agents/`（Agent 与业务分析）

### A2. 客流数字被放大最多 51 倍（用 `SUM` 聚合累计值）

**问题**
`retail_stats` 的 `visit_count` / `deep_interest_count` / `total_dwell_seconds`
都是**累计值**（视频管线每 75 帧写一次当前累计快照，只会涨、会话重启才归零），
不是"这一分钟的增量"。而 `agents/traffic_analytics.py` 的 `hourly_traffic()` 与
`zone_depth()` 用 **`SUM`** 聚合——等于把同一个值按采样密度**重复相加**。

**复现（真实数据，按小时对比新旧口径）**

| 小时 | 该小时行数 | 旧 SUM 口径 | 新口径 | 放大倍数 |
|---|---|---|---|---|
| 08-18 00:00~16:00 | 3 | 44 | 44 | **1.0x**（无放大） |
| 08-18 17:00 | 84 | 29 | 5 | **5.8x** |
| 08-18 19:00 | 180 | **1801** | 58 | **31.1x** |
| 08-18 22:00 | 153 | **459** | 9 | **51.0x** |

原始序列佐证（shelf_B 18:00~18:15）：`2,2,2,2,2,2,2,2,2,4,4,4,4,4,4,4,4`
→ `SUM = 2×9 + 4×8 = 50`，而 `MAX = 4`，**真实增量只有 2**。

**根因**
**同一份数据存在两套聚合口径**：`mysql_db.get_retail_stats_by_zone` 用的是 `MAX` ✓，
而 `traffic_analytics` 自己写了 SQL 用 `SUM` ✗——同一个指标两个答案。

**修复**
改为**两层聚合**：先按 `(小时, 区域)` 取窗口内峰值，再跨区域求和。
`zone_depth()` 同理改为 `MAX(visit_count/deep_interest/total_dwell)`。
并把口径原因写进 SQL 注释，注明"**曾经写错，务必别改回去**"。

**验证**
23 个小时逐一对上：采样稀疏时两者相同（1.0x），采样密集时新口径显著更低
（最大 **51.0x**）→ 与"放大倍数正比于采样密度"的预期一致 ✓

> **验证方法上的两个坑（都踩过，记下来）**：
> 1. 第一次验证选了 08-17 的数据——那天**每小时每区域只有 1 行**，两种口径必然相等，
>    比不出任何差异，却因为断言写得太松被判成 PASS（**假通过**）
> 2. 第二次验证 SQL 里的 `DATE_FORMAT` 格式串写错：**不传参数时 `%` 不能写成 `%%`**，
>    传参数时**必须**写成 `%%`（pymysql 会做 `query % args`），两次刚好写反
>
> **教训：验证"数值被放大"这类问题，必须先把数据特征（采样密度）本身查清楚，
> 否则很容易选到"测不出差异"的样本，然后得到一个假的 PASS。**

### A3. 客流同期对比永久为 0（`period_key` 粒度错配）

**问题**
`retail_stats.period_key` 是 **12 位分钟**（`YYYYMMDDHHMM`，每 75 帧一条快照，
为的是每分钟能 UPSERT 一行），而 `agents/period_compare.py` 生成的是
**10 位小时** key（`YYYYMMDDHH`）。`mysql_db.get_retail_stats_by_zone(period_key=X)`
用的是**精确匹配** `WHERE period_key=%s` → 拿小时 key 查热度**永远 0 行**。

导致"今天客流比昨天怎么样"从来拿不到客流数据，而 `SYSTEM_PROMPT` 又禁止回答
"没有历史数据"——只能答出"缺少对比基线"。

**复现（真实数据，同一时刻）**
```
period_key=2026091314   -> retail_stats 命中 0 行   （而 product_sales 有该 key）
compare_periods(2026-09-13 14:00) -> 客流 visit_count = 0，销量 sold_count = 5
```

**根因**
`period_compare.py:28` 的注释自称 *"period_key 口径：与视频管线写入保持一致（YYYYMMDDHH）"*
——**这个断言本身就是错的**：视频管线写的是分钟。作者基于一个错误前提实现了对比逻辑，
所以代码"看起来自洽"，实际永远查不到数据。

**修复**
**不改写路径**（它需要每分钟一行做 UPSERT），而是让查询端兼容两种粒度：
`mysql_db.get_retail_stats_by_zone` 在 key 长度 < 12 时改用**前缀匹配**
（`WHERE period_key LIKE 'YYYYMMDDHH%'`），配合已有的 `MAX(visit_count)`
即得到该小时末的累计值。同时**改正 `period_compare.py` 的错误注释**，
写明两种粒度并存的事实与兼容方式。

**验证**
```
period_key=2026081817   -> 命中 3 行（修复前恒为 0）✓
period_key=202608181732 -> 命中 3 行（精确匹配仍正常）✓
compare_periods(2026-08-18 17:30) -> 当前客流 = 5（修复前为 0）✓
```

> **方法论**：这条 bug 的根因不是"代码写错"，而是**注释写了一个错误的断言，
> 后续逻辑全都建立在这个错误前提上**。所以排查"功能静默失效"时，
> 要顺手核对代码注释里的口径声明是否与真实数据一致——**别信注释，去查数据**。

### A6. "转化率"可超 100%（单位错：件数当成了成交笔数）

**问题**
`sold_count` 是**件数**、`visit` 是**人次**，`sold / visit * 100` 严格来说是「**件/人比**」，
只有在 `sold <= visit` 时才近似"转化率"。而 `sales_analytics` 与 `traffic_analytics`
两处都一律叫"转化率"，并据此判定结论。

**复现（真实数据）**
```
shelf_B: visit=60 人次, sold=90 件 -> conversion_rate = 150.0%
诊断文本：转化率 150% 高但客流偏低：商品有竞争力...
```
"转化率 150%"本身自相矛盾（转化率不可能 >100%）；更早的子代理复现里还有
`10 人次 / 45 件 -> 450%` 被判成"吸引与转化俱佳"。

**根因**
单位混淆：数据层只记录了**件数**，没有成交**笔数**，所以真实转化率在现有 schema 下
**根本无法计算**，但代码按"可以算"来写并给出了确定性结论。

**修复**（不改字段名以兼容前端，但把口径写清楚）
- 两处都按实际大小**动态取标签**：`sold > visit` 时叫「件/人比」，否则才叫「转化率」
- `zones[]` 新增 `conversion_note` 字段，>100% 时明确说明
  "销量按**件**计、客流按**人次**计，N 件 / M 人次 = X% 属「件/人比」而非转化率；
  真实转化率需按成交**笔数**统计，当前数据未提供"

**验证（真实数据）**
```
shelf_B   visit=60  sold=90  conv=150.0  note=销量按**件**计、客流按**人次**计，
                                         90 件 / 60 人次 = 150% 属「件/人比」...
含口径说明的区域: shelf_B、shelf_D  -> PASS
```

> **方法论**：比"数字算错"更隐蔽的是**"数字算对了但单位不对"**——
> 公式没写错、代码没报错，只是把"件/人"叫成了"转化率"，于是一个不可能的值
> 被当成业务事实去下结论。**下确定性结论前先问一句"这个量纲可能吗"。**

### A7. 断流写下的 0 被当成"客流低谷"，并据此给出排班建议

**问题**
断流/摄像头未启动时，视频管线**同样会写下 `visit_count=0` 的行**，与"那个时段真的
没客人"在当前 schema 下**无法区分**。而 `hourly_traffic` 直接用
`min(hours, key=visit_count)` 选低谷 → 低谷永远落在"未采集"的时段上，
还会输出"低谷时段安排陈列调整"这种误导性建议。

**复现（构造含断流 0 的数据）**
```
09:00 -> 30 人次（正常）
10:00 -> 0        （断流写下）
11:00 -> 12 人次（正常）
修复前：valley = 10:00 (0 人次)   ← 把"没采集到"当成"客流最低"
```

**根因**
只做了数学上的 min/max，没做**数据可用性判断**——即本项目的"数据可信度门禁"
（第 4 项）在这条链路上缺席。

**修复**
- 高峰/低谷只在**有客流的时段**里选（`visit_count > 0`）
- 输出 `zero_hours` 计数；有 0 值小时时在 summary 里明确提示
  "可能是未采集（设备未运行/断流），也可能确实无客人，已排除出高低谷判定"
- 全部为 0 时直接返回"请先确认摄像头/视频源是否正常运行——统计值为 0 不代表真实客流"，
  而不是编出一个高低谷结论

**验证**
```
valley = 11:00 (12 人次)   peak = 09:00 (30 人次)   zero_hours = 1
summary 末尾: 另有 1 个小时无客流记录——可能是未采集（设备未运行/断流），
             也可能确实无客人，已排除出高低谷判定，建议核对摄像头运行情况。  -> PASS
```

### A4. 问答链路没有数据可信度门禁 —— 摄像头没开也答"到访 0 人次""未发现异常"

**问题**
`report_agent`（主动汇报链路）**早就有**这道门禁，会明确输出
"【数据不可信】…本次不出运营结论"；而**问答链路完全没有**，
于是同一个 0 在两条链路上有两种解释。

**复现（无任何视频帧时，调用 `quick_answer`）**
```
[quick_answer/popularity] 当前最热的是1号货架，全店累计到访 0 人次，疑似店员 0 人…
[quick_answer/anomaly]    当前共有 0 起可疑行为告警…目前未发现异常。
[quick_answer/emotion]    目前还没有识别到足够的人脸表情数据…
[report_agent 模板]       【数据不可信】…本次不出运营结论，请检查摄像头/视频源后重试。
```
`_fallback_answer`（LLM 不通时的兜底）同样漏了门禁：
`"当前累计 0 人次对货架商品感兴趣。 暂无异常告警。"`

**根因**
门禁逻辑**只接在了主动汇报链路上，没有接到问答链路**。危险在于：
告警场景下"0 起告警"被读成"未发现异常"等于**谎报平安**；
热度场景下"到访 0 人次"会被店长当成"今天没生意"。

**修复**
- `agents/master_agent.py` 新增模块级辅助 `_quality_gate(source)`：
  门禁本身不能成为故障点——任何异常都当作"数据可信"放行，保持原有行为
- 接进 `quick_answer` 的 **popularity / anomaly / emotion** 三个分支
  与 `_fallback_answer`
- 命中门禁时**不出业务结论**，改为明确说明并给出下一步动作，例如：
  "本次不出告警结论——「0 起告警」不等于「未发现异常」，请先确认视频源"

**验证**
```
popularity  门禁前缀=True  把0当事实陈述=False  -> PASS
anomaly     门禁前缀=True  把0当事实陈述=False  -> PASS
emotion     门禁前缀=True  把0当事实陈述=False  -> PASS
_fallback   门禁前缀=True  把0当事实陈述=False  -> PASS
不误伤检查（retail 源新鲜时应正常作答）        -> PASS
```

> **验证方法上的坑（第二次踩同一类）**：第一版断言写的是
> `"未发现异常" not in ans`，结果匹配到了**我自己新写的提示语里的否定句**
> （"「0 起告警」不等于「未发现异常」"）→ 假 FAIL。
> **断言不要用"某个词是否出现"，要用"是否以结论句形式陈述了那个事实"**，
> 否则很容易被自己新加的文案反噬。

### A5. 可信度按"任一路新鲜"判定 —— 浏览器推帧能掩盖服务器摄像头停摆

**问题**
`data_quality.snapshot()` 的聚合口径是"**任一路新鲜即为可信**"：

```python
if any(not s["stale"] for s in sources.values()):
    return {"reliable": True, ...}
```

而 `SOURCE_CLIENT`（浏览器/小程序推帧）与 `SOURCE_RETAIL`（服务器摄像头）
是两个独立的源。于是：**只要有人在浏览器里推帧，服务器摄像头即使完全停摆，
整个系统也会被判为"数据可信"**，业务侧继续把 0 当结论输出。

**复现**
```
各源: {'client': 新鲜}      -> 聚合 reliable = True
                              而 retail 这一路其实根本没有帧
```

**根因**
把"系统整体在收数据"（运维视角）与"这份业务数字可信"（业务视角）
混用了一个字段。多路场景下"不因某一路空闲而整体判死"的初衷是合理的，
但**业务结论必须看它依赖的那一路**。

**修复**
- `snapshot()` 每个源新增 `reliable` 布尔，并输出
  `reliable_by_source` / `unreliable_sources`；顶层 `reliable` 保留聚合口径
  （向后兼容），并在 docstring 里明确写出"业务侧别直接用"
- `is_reliable(source=None)` / `warning_text(source=None)` 支持按源判定；
  当"其它源仍在推送"时，提示语**点名是哪一路缺数据**：
  > 【数据不可信】服务器摄像头（零售分析）当前没有视频帧（注意：本机摄像头（客户端推帧）
  > 仍在推送，但本次结论依赖的是服务器摄像头（零售分析））。统计值 0 不代表真实客流/无异常。
- **两处调用点一并修正**（只改 data_quality 是不够的）：
  - `agents/report_agent.py` 的门禁改用 `reliable_by_source["retail"]`（保留旧字段回退）
  - `api/routes/report.py` 的 `/api/reports/data-quality` 顶层 `reliable`
    改为零售源口径并加 `reliable_scope: "retail"`——**这个接口是给仪表盘判断
    "业务数字能不能信"用的**，用聚合口径会导致预警横幅不出现

**验证**
```
聚合口径 reliable     = True   （向后兼容，故意保持）
is_reliable('retail') = False  （业务侧按源判定，正确）
warning_text('retail')= 【数据不可信】…本次结论依赖的是服务器摄像头…  -> PASS
[仅 client 新鲜] quick_answer('popularity') -> 命中门禁 -> PASS
```

> **方法论**：同一个"健康"字段被两种视角共用时，一定会出问题——
> **运维视角问"还在收数据吗"，业务视角问"这份数字能信吗"**，
> 两者的答案在多路场景下可以完全不同。设计时就该把它们分开。

---


### A13. 来源判定失败被伪装成"已确认真实"（提示变空串）

**问题**
`sales_analytics` 在来源判定整体失败时走：
```python
except Exception:
    sales_source, caveat = "unknown", ""
```
而 `source_caveat()` 对未覆盖的取值（含 `"unknown"`）也返回空串。
**空串的语义是"已确认为真实数据、无需提示"**——于是"来源无法确认"与
"已确认真实"完全无法区分。后果：结论可能建立在演示/测试数据上却毫无提示。

**复现**
```
sales_source = unknown
data_caveat  = ''          <- 与真实 POS 数据的输出一模一样
```

**修复**
- `sales_ingest` 新增 `SOURCE_UNKNOWN = "unknown"`（明确定义"判定失败"这一态）
- `source_caveat(unknown)` 返回**非空**提示（说明无法判断真实/演示、建议核对
  数据库连接与 `product_sales.source` 字段）
- `sales_analytics` 的异常分支同样给出非空 caveat

**验证**
```
source_caveat('unknown') 长度 = 97  （非空）
source_caveat('pos')     长度 = 0   （真实数据仍无提示，未误伤）
[来源判定失败] sales_source=unknown，data_caveat 非空 -> PASS
[正常路径]     sales_source=mixed，caveat 正常         -> PASS
```

### A15. 数据库故障被吞成"没有数据"，并误诊为摄像头故障

**问题**
`period_compare._aggregate` 里两个 `try/except` 都把异常吞成空列表，
于是 `has_data=False`，结论变成"当前时段没有采集到数据…**请先确认摄像头/视频源是否正常**"
——**把数据库故障误诊成摄像头故障**，让人去查一个没问题的地方。

**复现（打桩让查询抛 `Can't connect to MySQL server`）**
修复前的输出与"真的没数据"**完全一致**，只有那句误导性的摄像头建议。

**根因**
**"取不到"与"取到了但是空的"共用一个空列表表示**。两者处置完全不同：
前者要查数据库，后者才要看摄像头。

**修复**
- `_aggregate` 记录错误并暴露 `available`（数据是否取到）与 `data_error`（错误摘要）
- `_conclude`：`available=False` 时明确说"取数失败"+ 提示检查数据库连接，
  **并显式写明"不要据此判断摄像头有问题"**
- `note` 同理区分两种情况
- （`ops_archive` 的主查询本来就没吞异常、会正确返回 `{"status":"error"}`，无需改）

**验证**
```
[取数失败]   available=False；明确说明取数失败、提示查数据库；
             且未复现原误导措辞「请先确认摄像头/视频源是否正常」      -> PASS
[确实没数据] available=True；保留原"没有采集到数据"文案
             （该场景下这条建议是对的）                              -> PASS
```

> ⚠ **这是"断言写错导致假 FAIL"的第三次**（前两次见 A4）。
> 本次断言写的是 `"摄像头" not in conclusion`，结果匹配到了**我自己新写的否定句**
> "不要据此判断摄像头/视频源有问题"。
> **规则：断言要判"是否复现了目标缺陷的语义"（这里是那句误导措辞），
> 而不是"某个词是否出现"——否则一定会被自己新加的文案反噬。**

### A10. 告警队列 `deque(maxlen=500)` 封顶 —— "异常突增"检测永久失效

**问题**
`skill_anomaly` 的告警明细存在 `deque(maxlen=ANOMALY_ALERT_QUEUE_MAX)`（500）里，
而 `get_alert_summary()` 的 `total_alerts` 是 `len(self.alerts)`。
于是累计超过 500 起之后该值**永远停在 500**。

**为什么这会让突增检测彻底失效**：`detect_surge()` 靠的是**两次观测的增量**，
而基线也来自同一个封顶值 → 两边都是 500 → `d_total = 0` 恒成立 →
**无论真实新增多少告警都不再触发突增**。

**复现（真实调用 `process()` 累计 1200 起）**
```
total_alerts（明细，受队列上限约束）  = 500     <- 封顶
再新增 10 起后：
  明细口径增量 = 0    <- 突增检测看不到任何变化
```

**根因**
**用"受上限约束的明细条数"充当"累计计数"**。明细队列为了控内存必须封顶，
而增量判定需要单调不减的计数——两个需求被同一个字段承担了。

**修复**
- `SkillAnomaly` 新增**不封顶**的累计计数 `_total_count` / `_high_count`
  （在告警入队处自增，`reset()` 时清零）
- `get_alert_summary()` 输出 `total_alerts_cumulative` / `high_risk_count_cumulative`，
  并保留原字段（明细条数）供展示用
- `report_agent` 新增 `_alert_levels()` 统一取**累计**口径（无新字段时回退旧字段）
- `skill_anomaly.py` 的注释里写明"队列只保存明细、增量判定看累计"

**验证**
```
total_alerts=500 而 total_alerts_cumulative=1200
再新增 10 起：明细增量=0（封顶后必然），累计增量=10  -> PASS
```

### A11. 突增基线初值为 0 且在进程内 —— 重启即误报一次"异常突增"

**问题**
`report_agent._baseline` 的初值是 `{"total_alerts": 0, "high_risk_count": 0}`，
而 `detect_surge()` 直接拿"当前水位 − 基线"判定。
于是**每次重启后的第一轮，都会把"本次启动之前累积的历史水位"当成一轮突增报出去**。

**复现**
```
重启首轮（历史水位 30/8）-> delta_total=30、delta_high=8 -> 误报"异常突增"
```

**根因**
把"第一次观测"当成了"从 0 增长到当前值"。**基线必须有一个"建立"过程**，
第一轮只应该记录水位、不应该做判定。

**修复**
- `_baseline` 增加 `ready` 标志；`detect_surge()` **首轮只建立基线并返回 None**，
  从第二轮起才比较
- `update_baseline()` 同样使用累计口径并置 `ready=True`
- `reset_baseline()` 一并复位 `ready`

**验证**
```
[重启首轮]       历史 30/8        -> None          （不再误报）
[第二轮·水位未变] 30/8            -> None          （不刷屏）
[真实增长]       30/8 -> 45/11    -> delta_high=3  （真突增仍能报）
```

> **残留局限（诚实说明）**：基线仍在**进程内**，多副本部署时每个副本各有一套，
> 会各报一次——这是"进程内状态需外置"的一部分（见附四 F7）；
> 本轮只修了"重启误报"，没有做持久化，因为那需要决定存哪（Redis 已常驻，可承接）。

### A20. 意图路由把"货架空了吗 / 要补货吗"劫持到热度（销量）模板

**问题**
`agents/intent_router.py` 的强词表 `_POP_STRONG` 里含**「补货」**。
"补货"是**分析类**诉求（缺货/要不要补），却被强词判定成"热度 → 销量模板"，
于是已注册的 `empty_shelf` 模块**问不到**，用户拿到的是"今日销量最高的商品"。

**复现（修复前）**
7 条补货类提问（"A区需要补货吗""货架空了吗""3号货架没货了"…）
→ **7/7 全部落到销量模板**；对照组（"销量最高的商品""热度排行"）模板正确。

**根因**
路由顺序：**强词判定在任何意图桶之前**。`_POP_STRONG` 收录"补货"是按
"关心补货 ⇒ 关心销量"的直觉加的，但真实语料里它几乎总是"缺货 / 要不要补"的分析诉求。

**修复**
1. 在强词判定**之前**，把 `补货/缺货/断货/没货/空了/卖完/要补` 归入**分析桶**；
2. 从 `_POP_STRONG` 移除 `补货`（避免两处语义打架）。

**验证**
- 7/7 补货类提问 → 分析桶（`general`），不再命中销量模板；
- 7/7 对照组（销量/热度类）→ 仍走原模板，**无回归**；
- 端到端回归：`POST /api/queries`「A区需要补货吗」→ 200，响应中**不再出现**
  "今日销量最高的…"字样。

> ⚠ 记录一次**验证方法的坑**：第一版断言写成 `assert "未发现异常" not in answer`，
> 结果被**自己新加的否定句**（"未发现异常数据"）判成 FAIL。
> 正确做法是断言**缺陷的原始误导短语**（此处为"今日销量最高的"），
> 而不是断言"某个正确词不出现"。同一坑在本项目已踩过 3 次。

---

### A12. 周期对比拿"本小时前 N 分钟"比"昨天整个小时"

**问题**
`compare_periods` 直接用**当前整点**（如 `2026091615`）取数，
而数据是按**分钟**物化的（`retail_stats.period_key` 12 位 = `YYYYMMDDHHMM`）。
15:05 提问时"本小时"其实只有 5 分钟样本（当日更早时只有 12 分钟），
却与"昨天 15:00–15:59 整小时"比 → 显示 **-93%** 之类的**假暴跌**。

**复现**
同一分钟内用 1 / 3 / 6 小时三个窗口调 `/api/analytics/period-compare`，
`current.period_key` 都是当前**整点**而非"当前分钟"，
基线全部是历史**整小时** → 分子分母口径不一致。

**根因**
`mysql_db.get_retail_stats_by_zone()` 只支持按小时截断（`period_key LIKE 'YYYYMMDDHH%'`），
**没有"上界"参数**；`period_compare` 只能拿整小时当分子，
"截断到当前分钟"在 SQL 层根本无法表达。

**修复**
1. `get_retail_stats_by_zone(period_key=None, hours=1, until_key=None)`：新增 `until_key`，
   生成 `period_key <= %s` 的**上界**条件；
2. `compare_periods`：`_aggregate(period_key, until_key=None)`，
   把**当前时段与每一个基线**都截到 `f"{key}{now.minute:02d}"` —— 分子分母同口径；
3. `note` 显式说明：**"销量是小时粒度、无法按分钟截断"**
   （`product_sales.period_key` 是 10 位小时，硬截会凭空少算），
   因此销量对比在小时初仍偏小，只能当趋势参考。

**验证**
- 三个窗口（1/3/6 小时）的 `current.period_key` **都截到同一分钟**（实测 `:30`）→ 口径一致；
- `note` 含"分钟/小时粒度"说明；
- 无数据时 `current.available=false` + `data_error`，`conclusion` 明确回
  "当前时段没有采集到数据，无法给出同期对比结论"，**不再输出 -93% 这类假结论**。

> 诚实边界：`available` / `data_error` 加在 **`current`** 上；
> `comparisons[]` 的每一项用已有字段 `has_data` 表达同一语义（未新增字段）。

---

## 模块 C · 交付与运维（Docker / CI / 配置）

### E1. CI 门禁的密钥变量名写错，导致"门禁"形同虚设

**问题**
`.github/workflows/eval-gate.yml` 的 `env` 里写的是 `DEEPSEEK_API_KEY`，
而**全项目读的是 `dazuoye_api`**（见 `config/settings.py`）——
全仓 grep `DEEPSEEK_API_KEY` **命中 0 次**。后果：CI 里 `LLM_API_KEY` 恒为空，
走 LLM 的用例必然失败或退化，"评测门禁"名存实亡。
而 README 却把"80 条评测集 + CI 门禁"列为质量亮点。

**根因**
又一次**"变量名不匹配"导致的静默失效**——本项目第 N 次踩这个坑
（另见 compose 漏传 `mysql_root`、容器内硬编码 `127.0.0.1`、`.env` 文档与实现不符）。

**复现/核查**
```
grep -r DEEPSEEK_API_KEY 全仓 -> 0 次
grep -r dazuoye_api      全仓 -> config/settings.py（唯一读取点）
```

**修复**
- `env` 改为 `dazuoye_api: ${{ secrets.DAZUOYE_API }}`，
  并在文件头写明"变量名必须与代码一致"以及这次踩坑的经过（防止后人改回去）
- **补上 MySQL service**：`evals/run_evals.py` 的 `query_tool_logs()` 是**直接**连 MySQL 的
  （不过 SQLite 回退），且 `evaluate()` 每个用例都会调用它——没有 MySQL，
  CI 报的是**连接错误**而不是"评测失败"，两者含义完全不同
- 在 workflow 注释里写明需要手动创建 secret：`DAZUOYE_API`

**验证**
```
YAML 语法校验通过（.github/workflows/eval-gate.yml）
```
> ⚠ **尚未在真实 CI 跑过一次**——需要在仓库 Settings 里创建 `DAZUOYE_API` secret
> 后手动 Run workflow 才能确认变绿。**在真跑通之前，README 里"CI 门禁"的说法
> 仍属未经验证**（这条已记在附四 E1）。

---

### E7. mysql healthcheck 用 `mysqladmin ping` —— 认证失败也返回成功

**问题**
`docker-compose.yml` 里 mysql 的 healthcheck 是 `mysqladmin ping`。
它只证明"mysqld 进程还能应答"：**认证失败时同样 exit 0**。
容器被判 healthy，而依赖它的 backend 拿到的是"连得上但用不了"的假象，
`depends_on: condition: service_healthy` 这道门形同虚设。

**复现（同一容器，两种探针对比）**

| 探针 | 口令正确 | 口令错误 |
|---|---|---|
| `mysqladmin ping` | exit 0 | **exit 0**（假阳性） |
| `mysql -e 'SELECT 1'` | exit 0 | **exit 1**（正确报错） |

**根因**
`mysqladmin ping` 的语义是"服务器是否响应"，**不是"我能否用它"**，
它在 `Access denied` 的情况下依然返回成功。

**修复**
healthcheck 改为真实查询（`$$` 转义避免被 compose 提前做变量替换）：
```yaml
test: ["CMD-SHELL", "mysql -h 127.0.0.1 -uroot -p\"$$MYSQL_ROOT_PASSWORD\" -e 'SELECT 1' >/dev/null 2>&1"]
```
口令取自容器内的 `MYSQL_ROOT_PASSWORD`（由 compose 注入），**不写死在文件里**。

**验证**
- 改后 mysql 仍正常 healthy（说明真的能查）；
- 反证：把口令改错 → 探针 exit 1 → 容器转 unhealthy
  —— 这正是我们想要的行为（E7 之前是 exit 0，永远 healthy）。

> 与既有的 `/api/health/ready`（条目 36）配合才完整：
> 容器层保"mysql 可用"，应用层保"mysql + 关键依赖都就绪"。

---

## 模块 D · `api/`（接口层与鉴权）

### B2. 权限矩阵形同虚设：4 个权限声明了，实际只校验 1 个

**问题**
`api/security.py` 定义了 4 个权限：
```python
PERMISSIONS = {
    ROLE_ROOT:     {"data:read", "data:write", "user:manage", "system:manage"},
    ROLE_PLATFORM: {"data:read", "data:write"},
}
```
但全项目 `require_perm(...)` **只被用了 4 次，且全是 `user:manage`**。
也就是说 **`data:write` 与 `system:manage` 从未被任何端点校验**。

**后果（platform 角色越权）**
platform 只应有 `data:read` / `data:write`，实际上却可以：
注册任意视频源摄像头、绑定视频输出、增删摄像头模块、
启停门店采集、启停表情摄像头、**改 ROI 配置**、
以及**录入/导入/伪造销量数据**（直接影响经营决策）。

**复现（真实请求，修复前）**
```
POST /api/cameras        -> 200   （platform 也能注册摄像头）
POST /api/cameras/active -> 200   （platform 也能绑定输出）
DELETE /api/emotion-cameras -> 200（platform 也能停采集）
```

**根因**
**权限矩阵只写了"声明"，没有落到"执行点"**——没有把每个端点与它所属的权限绑定。
`PERMISSIONS` 看起来是一套完整的 RBAC，实际上是一份没有生效的文档。

**修复（按端点语义归类，逐个绑权限）**

| 权限 | 覆盖端点 | 判定理由 |
|---|---|---|
| `system:manage` | `POST/DELETE /api/cameras`、`POST /api/cameras/active`、`POST/DELETE/PUT /api/cameras/{id}/modules*`、`POST /api/streams/*`、`POST/DELETE /api/emotion-cameras` 及其 legacy 别名、`PUT/DELETE /api/zones` | 都是**系统级**：设备注册、采集启停、ROI 配置——影响整个系统行为 |
| `data:write` | `/api/analytics/sales-records{,/batch}`、`/sales/import`、`/sales/scan-inbox`、`/sales-simulate`、`/traffic-simulate`、`/paths-simulate` | 都是**业务数据写入**，可篡改经营依据 |
| 不加门禁（登录即可） | 所有读取类接口（列表/统计/报表/地图/问答） | 对应 `data:read`，登录本身已保证 |

**过程中的两个坑（都差点让修复失效）**

1. **legacy 别名会绕过门禁**：`emotion_camera.py` 的旧路径别名是
   `return await start_emotion_camera()` —— **直接调用**主函数，
   **不经过 FastAPI 的依赖注入**，所以只给主函数加 `Depends` 对别名完全无效。
   必须在别名自己的签名上再加一次。
2. **`Depends(require_perm(...))` 在函数定义时求值**：因此 `require_perm`
   必须**模块级导入**（不能像 `get_optional_user` 那样在函数内局部导入），
   否则导入期就 `NameError`。

**验证（真实 HTTP，root 与 platform 对照）**

对**写接口故意发非法 body**：被拒得 403（门禁先于业务校验）、放行得 422（已过门禁进入参数校验），
这样既能验证门禁、又不会真的注册摄像头或写销量。

```
操作                  期望      root   platform  判定
注册摄像头            应拒绝    422    403       PASS
绑定输出摄像头        应拒绝    404    403       PASS
停止采集              应拒绝    200    403       PASS
启动全部采集          应拒绝    200    403       PASS
停止表情摄像头        应拒绝    200    403       PASS
改 ROI 配置           应拒绝    422    403       PASS
录入销量              应放行    422    422       PASS
生成演示销量          应放行    200    200       PASS
查看摄像头列表        应放行    200    200       PASS
看客流统计            应放行    200    200       PASS
```
门禁点从 **4 个（全 user:manage）** 增加到 **26 个**，覆盖三类权限。

> ⚠ **诚实记录：这次验证有副作用，而我在脚本注释里写了"不产生任何副作用"——说错了。**
> `POST /api/streams/start-all` 与 `/sales-simulate` 都是**会真正执行**的写接口，
> root 调用时确实生效了。实际影响与处置：
> - 多路引擎被启动（3 路视频源在容器内都不存在，全部打不开、未真正运行）
>   → 已逐路 `stop`，`status` 回到 `{'streams': []}`
> - `sales-simulate` 返回 200 但**未写入任何行**（`product_sales` 仍 13 行）
> - `DELETE /api/emotion-cameras`：表情摄像头本就没运行，无影响
>
> **教训：用"故意发非法 body"来避免副作用，前提是那些接口确实会在校验阶段失败。
> 对于「无 body / 参数合法」的端点（start-all、simulate），非法 body 这招不成立。**
> 验证脚本必须先确认每个用例的副作用，或者干脆在隔离环境跑。



### B7. 一个安全响应头都没有（CSP / X-Frame-Options / nosniff 全缺）

**问题**
全站不加任何安全响应头：没有 `Content-Security-Policy`、`X-Frame-Options`、
`X-Content-Type-Options`、`Referrer-Policy`、`Permissions-Policy`，
https 下也没有 `Strict-Transport-Security`。页面可被任意站点 iframe 嵌套（点击劫持），
摄像头/麦克风权限没有显式约束，MIME 嗅探也没有被关掉。

**复现（修复前）**
`curl -I http://127.0.0.1:8000/api/health` → 只有 `content-type` / `content-length` 等，
**无任何安全头**。

**根因**
从未添加过相关中间件；FastAPI/Starlette **默认不下发**这些头（不是配置漏了，是从来没配）。

**修复**（`api/main.py` 新增 HTTP 中间件 `security_headers`）
| 头 | 值 | 作用 |
|---|---|---|
| `X-Content-Type-Options` | `nosniff` | 关掉 MIME 嗅探 |
| `X-Frame-Options` | `DENY` | 禁 iframe 嵌套（防点击劫持） |
| `Referrer-Policy` | `no-referrer` | 不外泄来源 URL |
| `Permissions-Policy` | `camera=(self), microphone=(self), geolocation=(), payment=()` | 摄像头/麦克风仅本站可用，定位与支付直接禁用 |
| `Content-Security-Policy` | `default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; media-src 'self' blob:; connect-src 'self' ws: wss:; font-src 'self' data:; object-src 'none'; base-uri 'self'; frame-ancestors 'none'` | 只允许同源脚本；`ws:/wss:` 放行视频与汇报通道 |
| `Strict-Transport-Security` | **仅 https 下下发** | 避免在纯 HTTP 部署时把浏览器锁死 |

**验证（含"会不会把自己打死"的兼容性核查）**
- 5 个头在**三类响应**上都存在：200 接口、静态资源 `/assets/*.js`、401 鉴权失败响应；
- **HTTP 下 HSTS 正确地不存在**（若误下发，浏览器会把纯 HTTP 站点记成 HSTS 后无法访问）；
- CSP 兼容性逐项排查（这一步是关键，CSP 加错会直接白屏）：
  - `index.html` **无内联 `<script>`**（只有 `<script type="module" src="/assets/...">`）；
  - 全仓 `onclick=` / `onchange=` 等**内联事件属性 0 处**（CSP 下会被拦）；
  - `eval(` / `new Function(` / `document.write` **0 处**（不需要 `unsafe-eval`）；
  - **无 CDN 依赖**（`script-src 'self'` 不会拦掉外部库）；
  - 实测页面 HTML + `/assets/index-*.js`(1.2MB) + CSS 均 200，正常加载。

> 诚实边界：`style-src` 保留了 `'unsafe-inline'` —— Vue 的运行时内联样式需要它，
> 未加 nonce/hash。这是**已知妥协**，不是遗漏。

---

### B10. 口令策略可被纯空白 / 超长口令绕过

**问题**
注册与改密只校验"长度 ≥ 8"：`"        "`（8 个空格）能通过，
`"A"×100000` 也能通过。前者是**无效口令**（用户以为设了密码，其实只是空白，
且很多客户端会 trim 后当成空），后者是一条**低成本的资源消耗路径**
（PBKDF2-HMAC-SHA256 **600000 轮** 会对超长输入做哈希 → 单请求 CPU 放大）。

**复现（修复前）**
```
POST /api/auth/users     {"password": "        "}   -> 201 创建成功
POST /api/auth/password  {"new_password": "A"*200}   -> 200 改密成功
```

**根因**
`_validate_password()` 只有一条 `len(password) < 8` 判断，
**没有上界**，也没有"必须含非空白字符"的判断。

**修复**（`api/security.py`）
```python
MAX_PASSWORD_LEN = 128
if len(password) < MIN_PASSWORD_LEN: ...        # 既有
if len(password) > MAX_PASSWORD_LEN: ...        # 新增上界
if not password.strip(): ...                    # 新增：不能只含空白字符
```
保留 8 位下限不变（**向后兼容**，不提高用户现有口令的强度要求）。

**验证**
- 纯空白 → 400（长度不足时提示"密码不能少于 8 位"；≥8 位空白时提示"密码不能只包含空白字符"）；
- 200 字符 → 400"密码最多 128 位"；
- **强口令仍可正常设置**（回归通过，未误伤合法口令）；
- 回归冒烟：两个用例均 400 且返回明确中文原因。

---

### C4. `_active_ws` 不含 `/ws/stream` —— 登记不一致（**但风险被审计高估**）

**问题（登记层面，真实存在）**
`api/routes/stream.py` 里 `/ws/stream` 在 `accept()` 之后**没有**把连接加入
`_active_ws`，导致 `shutdown_websockets()`（lifespan 关闭时统一断开）遍历不到它。

**复现与实测结论（关键：审计给的"风险"不成立）**
审计条目标注的影响是"关服务时关不掉主视频 WS、uvicorn 会一直等优雅关闭"。
实测（连上 `/api/ws/stream` 后 `docker compose stop backend`）：

| 实验 | 结果 |
|---|---|
| 默认 `timeout_graceful_shutdown=5` | 客户端收到 **1012 (service restart)** |
| 容器内临时改成 **20**（改前/改后对比） | 仍是 **1012**，**与宽限期无关** |

查 uvicorn 0.53 源码后原因明确：
```python
# uvicorn/protocols/websockets/websockets_impl.py
def shutdown(self) -> None:
    self.ws_server.closing = True
    if self.handshake_completed_event.is_set():
        self.fail_connection(1012)      # ← 关闭第一步就直接强断
    self.transport.close()
```
`Server.shutdown()` 的顺序是：停止接收 → **对所有连接 `connection.shutdown()`（WS 立即 1012）**
→ `wait_for(_wait_tasks_to_complete(), timeout=timeout_graceful_shutdown)`
→ **最后**才 `await lifespan.shutdown()`。

**推论（两条，都写清楚）**
1. `stream.py` 里 `ws.close(code=1001, reason="server shutdown")` 在**真实关停流程中执行不到** ——
   连接早被 uvicorn 以 1012 断掉，lifespan 的 `shutdown_websockets()` 跑在更后面；
2. **1012 是 uvicorn 的设计行为、也是标准关闭码**，客户端本就应"收到即重连"；
   已确认前端 `stream.js` 有**指数退避重连**（上限 10s）且会重发上次动作，
   汇报 WS（`App.vue`）同样会重连 —— **用户侧无感知，不构成"关不掉"的风险**。

**修复**
仍补上登记（`_active_ws.add(websocket)`，紧跟在 `accept()` 之后）：
- 它修的是**真实的不一致**（任何按 `_active_ws` 广播/统计的代码都会漏掉主视频流通道）；
- 但**不声称它改变了关停行为**——行为由 uvicorn 决定，本次实测已证明。

**验证**
- 登记后重新连接-关停实测：客户端仍是 1012（**与修复前一致**，符合上面的结论）；
- 反证"宽限期不是原因"：5s → 20s 结果不变；
- 容器内临时补丁**已还原**（`timeout_graceful_shutdown` 回到 5），
  容器内 `run.py` 与镜像一致。

> 这是**审计条目被高估**的一个实例，值得在面试里讲：
> "看到风险描述后我没有直接照着改就宣布修好，而是先复现 —— 结果发现真正的机制不是我猜的那个，
> 于是把结论改成'补一致性问题、不夸大成风险修复'。"

---

## 模块 E · 前端与小程序（`frontend-vue/` / `miniprogram/`）

### D1. `voice.js` 在仪表盘挂载**之前**执行 → 顶层 `addEventListener` 抛错

**问题**
`frontend-vue/public/js/voice.js` 是 IIFE，**顶层**就 `document.getElementById('btn-voice')`
并 `addEventListener`。它由 `Root.vue` 的 `enterApp()` 通过 `loadScriptOnce('/js/voice.js')` 加载，
而加载时机在**仪表盘挂载之前** → `btnVoice` 为 `null` →
`TypeError: Cannot read properties of null (reading 'addEventListener')`
**中断整个 IIFE**：语音唤醒/识别整体失效，控制台只留一条容易被忽略的报错。

**复现（Node + DOM 桩，改前/改后对照）**
| 场景 | HEAD 旧版 | 本次修复版 |
|---|---|---|
| `#btn-voice` 不存在 | **TypeError 抛出，IIFE 中断** | 无异常，1 条 `console.warn` |
| DOM 齐全 | 正常绑定 3 个监听 | 正常绑定 3 个监听（**无回归**） |

**根因**
两处叠加：
1. `Root.vue` 里 `loadScriptOnce()` 调用在 `this.phase = 'ready'`（触发仪表盘渲染）**之前**，
   而此时 DOM 尚未挂载；
2. `voice.js` 自身没有任何"元素不存在就退出"的守卫，一抛错就整段脚本失效。

**修复**
1. `Root.vue`：`enterApp()` 改为 **先 `this.phase = 'ready'` → `await this.$nextTick()`（等 DOM 就绪）
   → 再 `await loadScriptOnce('/js/voice.js')`**；
2. `voice.js`：顶层加防御性守卫，取不到 `#btn-voice` 时**只告警并 `return`**
   （即使将来被提前加载/缓存顺序变化，也不会静默崩溃）。

**验证**
- 上述对照实验（旧版抛 TypeError / 新版干净告警）；
- DOM 齐全时行为完全一致（绑定 3 个监听、无警告）→ 无回归；
- 产物核查：容器内 `dist/js/voice.js` 与源码 `public/js/voice.js` **md5 一致**
  （`5a81204b4532f92e5d732a996b75259e`），
  且编译产物 `assets/index-D3BpqCL7.js` 内 `enterApp` 顺序确认为
  `this.phase='ready', await this.$nextTick(), await $o('/js/voice.js')` → **改动确实生效**。

---

### D2. 小程序发 `{type:'start_webcam'}`，后端只认 `{action:...}`

**问题**
`miniprogram/pages/video/video.js` 打开摄像头时发送 `{"type": "start_webcam"}`，
而 `/api/ws/stream` 的服务端协议是读 `msg["action"]`（见 `api/routes/stream.py`）。
字段名不匹配 → 服务端收到不认识的报文 → **监控页永远收不到画面**
（前端显示"已连接"但画布全黑），且没有任何报错提示，极难排查。

**复现（修复前）**
连上 WS 后分别发送两种报文：
```
{"type":"start_webcam"}                        -> 无任何响应
{"action":"start_webcam","camera_id":0,...}    -> 正常进入取流流程
```

**根因**
小程序端协议与后端**各自演进**，`type` 是更早的报文格式残留；
后端只认 `action`，对未知报文**静默忽略**（没有"未知指令"回包）——这是放大排查难度的第二个原因。

**修复**
`video.js` 改为与 Web 端一致的协议，并补齐参数：
```js
{ action: 'start_webcam', camera_id: 0, mode: 'retail' }
```

**验证**
- `{"type":"start_webcam"}` 仍无响应（反证后端确实只认 `action`）；
- 新报文 → 服务端返回 `{"status":"error","message":"无法打开摄像头 #0"}`
  —— 这是**预期的正确行为**：报文已被正确解析并进入取流逻辑，
  容器环境里没有 0 号设备所以打开失败（真机上有设备即正常出图）。

> 遗留（未修，属功能缺口）：后端对**未知 action** 静默忽略，
> 建议补一条 `{"status":"error","message":"未知指令: xxx"}`，避免同类协议漂移再次静默失败。

---



# 附一、验证方法速查

本文档所有"验证"均可复现，常用手段：

| 手段 | 适用 | 例 |
|---|---|---|
| **打桩 + 断言** | 确定性逻辑（对比计算、来源判定、状态机） | 桩掉 `mysql_db` 验证变化率 |
| **HTTP 契约测试** | 接口层（状态码/错误分支/落库） | `TestClient` + 真实路由，验证 400/403 分支 |
| **真实数据源只读集成** | 新能力在真实 MySQL 上的表现 | `period_compare` 读真实库不崩 |
| **真实写入回读** | 写入链路端到端 | 导入销量 → 读回 → 判定来源 |
| **Node 加载真实模块** | 小程序 JS 逻辑 | 桩 `wx`/`getApp` 后跑 `api.js` 真实文件 |
| **项目自带评测框架** | 端到端回归（真实技能） | 跑 router 模板路径用例（零 LLM 成本） |
| **全新环境引导路径** | 只有首次部署才走的路径 | 独立测试库复刻 lifespan 顺序 |
| **容器内端到端** | 镜像能否真的跑起来 | 起容器打真实 HTTP，一次性验 12 项 |
| **哨兵文件测忽略规则** | `.dockerignore` / `.gitignore` 是否真生效 | 放假 `.env` 真跑构建，容器内判 `test -f` |
| **一次性卷跑真编排** | 编排层（变量传递 / 服务名 DNS / 健康门控） | `-p retailtest` + `*_test` 卷，不碰生产容器与数据 |
| **删依赖后自证功能** | 确认删除依赖是安全的 | 移除 ffmpeg 后仍能解码 mp4 / 读 wav |
| **A/B 对照实验** | 确认"某单一因素"是否真是原因 | 同内容 LF vs CRLF：一个构建成功、一个解析报错 |
| **改属性后重新 checkout** | 验证 `.gitattributes` 等仓库行为 | 删文件 → `git checkout` → 查字节确认换行 |
| **系统性变量比对** | 找出"迁移后静默失效"的功能 | 正则提取全部 env 名 → 比对宿主/容器求差集（37 个里抓出 4 个遗漏） |
| **服务端反查** | 确认"上报/写入"真的成功 | 发 trace 后调 langfuse API 查回，而非只看"没抛异常" |
| **降级路径正向触发** | 确认静默降级没被当成正常 | 直接调 `langfuse_available()`，而非看日志有无报错 |

**经验**：验证时**先把"失败是不是产品 bug"分清**。本轮多次出现"测试脚本断言写错"（如把 `+25%` 的用例写成期望下降、忘了表头占第 1 行、LangChain `@tool` 返回的 `StructuredTool` 不是可调用对象）——**这些是测试的问题，不是代码的问题**，但每一次都要实际确认过才算数。

---

# 附二、面试可讲的技术点

1. **鉴权为什么要服务端会话而非 JWT**：可即时吊销（改密码/停用立即生效）、无需黑名单；且鉴权库独立于业务库，避免业务库故障把管理员锁在门外
2. **为什么鉴权中间件必须用纯 ASGI**：`BaseHTTPMiddleware` 拦不到 WebSocket——视频流/推送通道会绕过鉴权
3. **WebSocket 的令牌只能放 query**：浏览器 WebSocket API 不支持自定义请求头，音频 URL 同理（所以 TTS 也必须 `?token=`）
4. **多端架构：按角色分权，不按端分权**：接口只有一套，端差异体现在前端渲染与传输适配（Cookie vs Bearer），**不为小程序复制后端接口**
5. **数据可信度是底线**：宁可说"没有数据"，也不把设备故障输出成业务结论
6. **演示数据必须标注来源**：用错数据改货架比没有数据更危险
7. **StateGraph 的价值在条件分支与降级可见**，重构时不改变对外契约（调用方零改动）
8. **单点数据没有决策价值**：同期对比才有；且"数据在库里"与"能查出来"是两回事（本轮就是补查询入口）
9. **压测的价值是找瓶颈，不是刷数字**：本次暴露的是同步持久化串行瓶颈（QPS 6 倍提升）；也要警惕**指标单位错标**这类"数据自相矛盾"的低级问题
10. **LLM Wiki / RAG 选型要匹配场景**：本项目 Agent 靠**工具查实时数据**作答，不靠知识检索，所以 RAG 优化与 LLM Wiki 都不是当前瓶颈（向量层召回的是**历史会话记忆**，不是领域知识）
11. **`.gitignore` 和 `.dockerignore` 是两套独立机制**：`COPY . .` 会把密钥烤进镜像层，且**该层永久留在镜像历史里**（事后删文件也挖得出来）。加了 `.gitignore` 完全不防 Docker——凡是 `COPY . .` 的项目，两份清单必须一起维护
12. **"依赖没人用"是删除的必要条件，不是充分条件**：grep 不到调用只能说明没有**显式**调用，证明不了库没有隐式依赖它。删完必须**自证功能仍工作**（本项目就是靠"无 ffmpeg 仍能解码真实 mp4/wav"才敢删）
13. **"镜像测通" ≠ "编排测通"**：我用 `docker run -e mysql_root` 测通了镜像，却**恰好掩盖了 compose 漏传该变量**的缺陷。更危险的是这个错配**不报错**——`/api/health` 仍返回 healthy、页面照常打开，只是 MySQL 一条数据都读不到。**静默降级比直接报错危险得多**
14. **compose 的 `${VAR}` 不等于"注入容器"**：它只是**宿主机侧的字符串替换**（用来拼 `MYSQL_ROOT_PASSWORD`）。变量要真正进入某个容器，只能靠该服务的 `environment:` 或 `env_file:`；并且用 `${VAR:?err}` 让缺失时**直接失败**，而不是悄悄降级
15. **容器化要把"必需"和"可选"分开**：只发布 backend 端口（数据库服务走 compose 网络用服务名互访），既避免端口冲突也少一个攻击面；实时性要求高的摄像头/GPU 推理留在边缘，容器跑业务侧——**不是所有东西都该进容器**
16. **安全地验证有状态系统**：用独立项目名（`-p retailtest`）+ 一次性卷跑真实编排，**全程不碰生产容器与卷**，测完 `down -v` 并**复查生产数据行数未变**。"测的是真东西"和"没搞坏真数据"这两步都不能省

---

# 附三、后续待办

> **注意**：本节是较早（v4 收尾阶段）整理的待办。之后又做了 5 路只读审计，
> 结果更完整地记在 **附四**（77 条，含可信度标注）。**排期请以附四为准**，
> 本节保留作为历史演进记录。

| 优先 | 事项 | 说明 |
|---|---|---|
| ✅ | **提交代码** | **已完成**：v4 仓库 `Retail_assistant_v4` 已创建并推送（v3 仓库保留作归档，历史未改动） |
| ✅ | 测试数据处置 | **已处理（改为「标注」而非删除）**：新增 `product_sales.source` 字段，测试数据标 `test`、演示数据标 `simulated`（见第 28 项）。保留数据便于演示，但**来源显式可查**，不会被误当真实数据 |
| 中 | 补 pytest 单元测试 | 现有 CI 门禁只验"跑完没崩"，缺断言级单测 |
| 中 | 告警处理闭环 | 无"已处理/误报"状态 → 无法用误报率反哺阈值 |
| 中 | 变化驱动的 LLM 汇报 | 现在固定 10 分钟一次 = 144 次/天/店；可加条件分支"无变化则跳过"，成本降一个量级 |
| 中 | 营业时间感知 | 闭店时段"客流 0"是正常的，会污染报表均值与归因 |
| 中 | 令牌不进 URL | ✅ **已完成**（见第八部分 / 第 27 项） |
| 低 | 前端 `innerHTML` 未转义 | `App.vue` 第 1061/1098 行把 `a.reasons` 未转义拼进 `innerHTML`；数据源可控但违背原则，建议统一 `textContent` |
| 低 | 地图缓存淘汰策略 | `clear()` 全清 → 改为按过期时间淘汰少量（避免缓存雪崩） |
| 低 | 多进程化前置 | 进程内状态需外置（`_MAP_CACHE`、限流信号量、`_active_camera`、汇报基线）；注意**登录限流已存 SQLite，多进程天然共享** |
| 低 | 小程序「用户管理」页 | `hasPerm('user:manage')` 已就绪，可把角色差异真正用起来 |

---

## 已完成的安全加固（原"待办"项）

| 原问题 | 状态 |
|---|---|
| 登录无限流/锁定 + 弱口令 | ✅ 已修（25.1、25.5） |
| Cookie 无 `Secure` | ✅ 已修（25.2） |
| CORS 反射任意 Origin + 凭据 | ✅ 已修（25.3） |
| `/docs`、`/openapi.json` 公开 | ✅ 已修（25.4） |
| `/api/health` 暴露设备指纹 | ✅ 已修（25.4） |
| 哈希算法核查 | ✅ 已确认是慢哈希 + 随机盐，迭代轮数提到 60 万（25.6） |
| 令牌进 URL（会落访问日志） | ✅ 已修：一次性票据 + 服务端拒绝 URL 主令牌（27） |
| `.env` 文档与实现不符 | ✅ 已修（26） |

> 说明：这些加固均已通过**真实请求逐项验证**（非静态审查），验证脚本为一次性用途，验完即清理。

---

## 仍未完成（需要你决定的）

| 优先 | 事项 | 为什么重要 |
|---|---|---|
| ✅ | **提交代码** | **已完成**：v4 仓库 `Retail_assistant_v4` 已推送；v3 仓库保留作归档。容器化改动待提交 |
| ✅ | 测试数据处置 | **已完成**：改为显式 `source` 标注（`test` / `simulated`）而非删除，见第 28 项 |
| ✅ | 容器化 | **已完成**：`Dockerfile` + `docker-compose.yml` + `.dockerignore`，见第 29 项 |
| 中 | 补 pytest 单元测试 | 现有 CI 门禁只验"跑完没崩"，缺断言级单测 |
| 中 | 告警处理闭环 | 无"已处理/误报"状态 → 无法用误报率反哺阈值 |
| 中 | 变化驱动的 LLM 汇报 | 现在固定 10 分钟一次 = 144 次/天/店；可加条件分支"无变化则跳过"，成本降一个量级 |
| 中 | 营业时间感知 | 闭店时段"客流 0"是正常的，会污染报表均值与归因 |
| 中 | **外部告警推送** | 现在告警只在页面/WS 内推送，**人不在电脑前就收不到**（企业微信/钉钉机器人是最贴业务的一块） |
| 低 | 前端 `innerHTML` 未转义 | `App.vue` 1061/1098 行把 `a.reasons` 未转义拼进 `innerHTML`；数据源可控但违背原则 |
| 低 | 地图缓存淘汰策略 | `clear()` 全清 → 改为按过期时间淘汰少量（避免缓存雪崩） |
| 低 | 多进程化前置 | 进程内状态需外置（`_MAP_CACHE`、限流信号量、`_active_camera`、汇报基线） |
| 低 | 小程序「用户管理」页 | `hasPerm('user:manage')` 已就绪 |
| 低 | 镜像瘦身 | 换 `opencv-python-headless` 可去掉 Mesa GL/LLVM（约 100MB+）；需处理 ultralytics 的依赖声明冲突 |
| ✅ | 构建提速 | **已完成**：`torch` 用 `--no-deps` 装、依赖交给清华源，pip 阶段 21.6 分钟 → 约 2.5 分钟（见 29.9） |
| — | **小程序真机验证** | 本次票据改造只能 Node 验逻辑；WS/音频实际连通性需你在微信开发者工具点一次 |
| ✅ | **compose 正式切换** | **已完成**：先备份（26.3MB）→ 解除 `restart=always` → 停旧容器 → `docker compose up -d`。数据完整保留，见 29.6 |

---

# 附四、审计发现的待办台账（2026-09 五路只读审计）

> **背景**：本轮做了 5 路并行**只读**审计（业务逻辑 / 安全 / 数据层与资源管理 / 前端与小程序 / 运维部署）。
>
> **阅读方式**：条目编号前带 **✅** 的是**已修复**的（第四列写"修复与验证见 **十一 · 模块 X**"，
> 可跳转查看五段式记录）；**不带 ✅** 的是**尚未修复**、供后续排期的部分。
> 早期几批修复（第十部分 29.17~29.23）记在第十部分。
>
> **可信度标注**（很重要，别把推断当结论）：
> - 🔬 **我已实测**：我本人复现过，有具体数字
> - 📋 **子代理实测**：子代理给出了复现证据，我**未逐条**独立复核
> - 📖 **仅静态推断**：只读代码得出，未运行验证

## A. 业务正确性 —— 优先做（数字本身不对，面试会被问穿）

| # | 问题 | 位置 | 影响 | 可信度 |
|---|---|---|---|---|
| ✅ A1 | ~~表情识别一检测到人脸就崩溃：`detect()` 缺 `return faces`~~ **已修** | `cv_engine/face_emotion.py` | 修复与验证见 **十一 · 模块 A** | 🔬 我实测复现 |
| ✅ A2 | ~~客流数字被放大 10 倍以上：`visit_count` 是累计快照却用 `SUM` 聚合~~ **已修**（实测最大放大 **51 倍**） | `agents/traffic_analytics.py:23,104` | 修复与验证见 **十一 · 模块 B** | 🔬 我实测 |
| ✅ A3 | ~~客流同期对比永久失效：`period_key` 粒度错配~~ **已修** | `agents/period_compare.py:28`、`mysql_db.get_retail_stats_by_zone` | 修复与验证见 **十一 · 模块 B** | 🔬 我实测 |
| ✅ A4 | ~~问答链路无数据可信度门禁~~ **已修**（见 十一 · 模块 B） | `agents/master_agent.py:743-758` | 摄像头没启动也回"到访 0 人次""未发现异常"（汇报链路有门禁，两条链路对同一个 0 的解释不一致） | 📋 |
| ✅ A5 | ~~可信度按「任一路源新鲜」判定~~ **已修**（见 十一 · 模块 B） | `agents/data_quality.py:83-90` | 打开浏览器摄像头推帧，就能把停摆的零售源判为"可信" | 📋 |
| ✅ A6 | ~~转化率 = 件数 / 人次，可超 100%~~ **已修** | `agents/sales_analytics.py:63` | 10 人次 / 45 件 → 450% 仍判"吸引与转化俱佳" | 📋 |
| ✅ A7 | ~~断流写下的 0 被当「客流低谷」~~ **已修** | `agents/traffic_analytics.py:39-55` | 据此给出排班/陈列建议 | 📋 |
| A8 | 演示数据混进真实时段分析且无来源标注 | `agents/traffic_analytics.py:106` | 销量侧有 `source`，热度侧没有 | 📋 |
| A9 | 补录历史批次被算进"最近 1 小时" | `agents/sales_ingest.py:161-168` | 未传 `period_start/end` → 默认填导入时刻 | 📋 |
| ✅ A10 | ~~告警队列 `deque(maxlen=500)` 封顶~~ **已修**（累计口径不封顶，见 十一 · 模块 B） | `skills/skill_anomaly.py:59` | 基线也封顶 → 突增检测永久失效 | 📋 |
| ✅ A11 | ~~突增基线在进程内、重启归零~~ **已修**（首轮只建基线；仍为进程内，多副本局限见 F7） | `agents/report_agent.py:31` | 重启后把历史存量误报为"异常突增" | 📋 |
| ✅ A12 | ~~同期对比拿"本小时前 N 分钟"比"昨天整个小时"~~ **已修**（当前与各基线统一截到当前分钟；销量为小时粒度、无法按分钟截断，已在 `note` 说明） | `agents/period_compare.py`、`mysql_db.get_retail_stats_by_zone` | 修复与验证见 **十一 · 模块 B** | 🔬 我实测 |
| ✅ A13 | ~~来源判定异常被吞成 `unknown` → 风险提示变空串~~ **已修**（见 十一 · 模块 B） | `agents/sales_analytics.py:38-51` | 与真实 POS 数据输出无法区分 | 📋 |
| A14 | 销量 `zone_id` 不与 ROI 校验 | `agents/sales_ingest.py:149-151` | `shelf_a` vs `shelf_A` → 幽灵区域 + 反向诊断 | 📋 |
| ✅ A15 | ~~DB 故障被吞成"没有数据"~~ **已修**（见 十一 · 模块 B；`ops_archive` 主查询本就没吞异常，无需改） | `agents/period_compare.py:36-43` | 与真的没数据完全一致，还会误导去查摄像头 | 📋 |
| A16 | 动线支持度可 >100%（口径与文档不符） | `agents/movement_analytics.py:46-59` | 按转移次数而非按轨迹去重 | 📋 |
| A17 | 空货架提醒用"自创建以来累计到访<3" | `skills/skill_new_modules.py:75` | 不是时间窗；零数据时两货架都报需补货 | 📋 |
| A18 | 门口客流"离开判定"用 30 帧而非秒 | `skills/skill_new_modules.py:43-51` | fps=5 时"在店"时长算错 6 倍 | 📋 |
| A19 | 表情趋势样本太少即下"明显下降"结论 | `skills/skill_emotion.py:78-119` | 9 neutral + 1 sad 就报"情绪明显下降" | 📋 |
| ✅ A20 | ~~意图路由把"货架空了吗/要补货吗"劫持到热度模板~~ **已修**（7/7 补货类提问归入分析桶，对照组无回归） | `agents/intent_router.py` | 修复与验证见 **十一 · 模块 B** | 🔬 我实测 |

## B. 安全

| # | 问题 | 位置 | 影响 | 可信度 |
|---|---|---|---|---|
| B1 | 任意登录账号可让服务端连任意 RTSP/UNC/绝对路径 | `api/routes/cameras.py:29-33` → `cv_engine/multi_stream.py:41-54` | SSRF + 本机任意可解码文件读取（Windows 下可外带 NTLM） | 📋 |
| ✅ B2 | ~~权限矩阵形同虚设：定义了 4 个权限，全项目**只用 `user:manage`**~~ **已修**（门禁点 4 → 26，见 十一 · 模块 D） | `api/security.py:57-60` | platform 角色可注册摄像头、改 ROI、伪造销量 | 📋 |
| B3 | **会话/对话数据无归属**（`chat_session`/`query_history` 无 user_id） | `mysql_db.py:198-207` | 任意登录账号可列出/搜索/删除**所有人**的问答记录 | 📋 |
| B4 | 改状态用 GET（CSRF）：`SameSite=Lax` 下顶层 GET 导航带 Cookie | `api/routes/emotion_camera.py` 的 `GET /emotion_camera/stop` | 诱导管理员点链接即可停掉分析 | 📋 |
| B5 | ECharts tooltip 用函数 formatter 拼 HTML，而 `zone_label` 任意账号可写 | `frontend-vue/public/js/chart.js:101-107` + `PUT /api/zones` 无权限校验 | 存储型 XSS（以受害者身份发请求） | 📋 |
| B6 | WS `start_file` 接受任意路径 | `api/routes/stream.py:732-736` | 任意可解码文件读取 + 存在性探测 | 📖 |
| ✅ B7 | ~~无安全响应头（CSP / X-Frame-Options / HSTS 全缺）~~ **已修**（5 个头，HSTS 仅 https；已逐项核查与现有前端兼容） | `api/main.py`（`security_headers` 中间件） | 修复与验证见 **十一 · 模块 D** | 🔬 我实测 |
| B8 | 异常原文回给客户端（泄露 DB 主机/驱动/内部结构） | `report.py`、`chat.py`、`query.py` 多处 | 📋（已核实口令未泄露） |
| B9 | 公开端点参数原样写日志 → 可伪造审计日志行 | `api/main.py:391-395` | 📋 |
| ✅ B10 | ~~口令策略可被纯空格 / 超长口令绕过~~ **已修**（`MAX_PASSWORD_LEN=128` + 拒绝纯空白） | `api/security.py` | 修复与验证见 **十一 · 模块 D** | 🔬 我实测 |
| B11 | 上传无大小限制、无内容校验、同名静默覆盖 | `api/main.py:423-488` | 📖（文件名净化本身实测安全） |
| B12 | 一次性票据不绑定来源、`purpose` 消费时不校验 | `api/security.py:595-613` | 📋 |
| B13 | Cookie 优先于 Bearer：垃圾 Cookie 会让有效 Bearer 失效 | `api/security.py:692-700` | 📋 |
| B14 | "非 `/api/` 前缀一律放行"的默认开放策略 | `api/security.py:801-812` | 📋（当前无实际暴露路径） |
| B15 | 人脸脱敏默认关闭（`SANITIZE_FACES=0`） | `config/settings.py:218` | 合规默认偏松（文档如实写了，故不算撒谎） | 📋 |

## C. 数据层与并发（本类已修：H1 连接池 / H3 线程死亡 / M1 串台 / M3 阻塞 / M4 seq_no / M5 ROI / M6 降级表 / M8 retired）

| # | 问题 | 位置 | 影响 | 可信度 |
|---|---|---|---|---|
| C1 | 23 个函数在异常路径漏 `close()` | `mysql_db.py` 多处 | **子代理实测：GC 会兜底归还，不是永久泄漏**；但降级为裸连接时没有池兜底 | 📋 |
| C2 | 静默降级把一份数据劈成两库 | `database.py:81-217` | 掉线期间写入 SQLite 的数据，MySQL 恢复后**读不到**（写入目标在运行期漂移） | 📖 |
| C3 | 单例懒加载全部无锁 | `api/dependencies.py:36-135` | 并发首连会重复加载模型（显存/内存翻倍） | 📖 |
| ✅ C4 | ~~`_active_ws` 不含 `/ws/stream`~~ **已补登记**（**风险经实测被高估**：uvicorn 关闭第一步即 `fail_connection(1012)`，与 `timeout_graceful_shutdown` 无关；前端已有指数退避重连） | `api/routes/stream.py` | 修复与实测结论见 **十一 · 模块 D** | 🔬 我实测（5s→20s 对照） |
| C5 | `join(1.0)` 后立刻 `cap.release()`，与仍在 `read()` 的线程并发 | `api/routes/stream.py:897-900` | OpenCV 非线程安全，属未定义行为 | 📖 |
| C6 | 无 schema 版本管理；每次启动对 `query_history` 做窗口函数全表回填 | `mysql_db.py:333-351` | 历史越长启动越慢；新增表无迁移探测 | 📖 |
| C7 | 迁移里的 `ADD UNIQUE KEY` 在已有重复数据上会失败 → 静默回退 SQLite | `mysql_db.py:284-294` | 升级路径可能无声换库 | 📖 |
| C8 | checkpointer 的 `list()` 无 LIMIT 全表读 + 1+N 查询 | `agents/mysql_checkpointer.py:357-362` | 📋 |
| C9 | 采集热路径在 Python 循环里逐条 INSERT | `mysql_db.py:809-837` | 区域/告警数线性放大 DB 往返 | 📖 |
| C10 | 向量层逐条 HTTP、无批量 upsert、用普通 Lock | `vector_memory.py:94-98,355-379` | 启动全量重建慢；事件循环可能阻塞在锁上 | 📖 |
| C11 | 多实例部署时定时任务重复执行（"幂等"只在进程内成立） | `scheduled_tasks.py`、`agents/sales_inbox.py` | N 副本 = N 份 LLM 汇报 + 销量 CSV 可能重复导入 | 📖 |

## D. 前端与小程序

| # | 问题 | 位置 | 影响 | 可信度 |
|---|---|---|---|---|
| ✅ D1 | ~~`voice.js` 在仪表盘挂载**之前**执行 → 顶层 `addEventListener` 抛错~~ **已修**（Root.vue 先挂载后加载 + voice.js 顶层守卫）；原影响：语音功能整体失效 | `frontend-vue/src/Root.vue`、`public/js/voice.js` | 修复与验证见 **十一 · 模块 E** | 🔬 我实测（DOM 桩前后对照） |
| ✅ D2 | ~~小程序 `video.js` 发 `{type:'start_webcam'}`，后端只认 `action`~~ **已修**（原影响：监控页永远收不到画面） | `miniprogram/pages/video/video.js` | 修复与验证见 **十一 · 模块 E** | 🔬 我实测 |
| D3 | 登录态过期后前端无 401 兜底（**Tier-1 期间已独立确认机制**） | `frontend-vue/public/js/stream.js:118-135`、`App.vue` 多处 | 面板静默停在旧数据 + WS 无限重连，**永不跳登录页**。机制：服务端对无票据/过期会话的 WS **先 accept 再以 1008 关闭**（`api/security.py:913`），而前端 `onclose` **完全不检查关闭码**，只按指数退避重连（上限 10s）→ **1008 也会无限重连**；汇报 WS 同理 | 🔬 服务端 1008 已实测 + 📖 前端 `onclose` 未判码（静态确定） |
| D4 | 小程序 `onUnload` 后自动重连 → 僵尸 WS | `miniprogram/pages/video/video.js:144-147` | 页面已销毁仍建连并 `setData` | 📋 |
| D5 | SSE 帧不转义换行 | `api/routes/query.py:187`、`master_agent.py:957` | 多行回答丢字；降级兜底路径几乎整段丢失 | 📋 |
| D6 | 登出不清 `chat_session_id` → 换账号串会话 | `App.vue:376-386` | 与 B3 叠加后能看到他人问答 | 📖 |
| D7 | 小程序 `voiceask.js` 多连接 + 定时器泄漏 | `pages/voiceask/voiceask.js:30-48,75` | 📋 |
| D8 | 摄像头切换竞态（表情模式未 `await stopVideo`） | `App.vue:417-428` | 刚启动的画面被自己的 stop 停掉 | 📖 |
| D9 | `startLocalCamera` 的 await 窗口内点"停止"，摄像头仍会启动 | `App.vue:465-497` | **摄像头未被释放**（隐私相关） | 📖 |
| D10 | ECharts 实例从不 `dispose()` | `App.vue:344-373`、`public/js/chart.js:27-29` | 重复挂载会叠加 resize 监听 | 📖 |
| D11 | 小程序 `app.js` 冷启动后权限恒为 `[]` | `miniprogram/app.js:24` | `hasPerm` 恒 false（当前各页未调用，属潜伏） | 📋 |
| D12 | 小程序视频页每帧 `setData`（约 30 次/秒） | `pages/video/video.js:161-169` | 📋 |

## E. 运维与部署

| # | 问题 | 位置 | 影响 | 可信度 |
|---|---|---|---|---|
| ✅ E1 | ~~CI 密钥变量名写错：workflow 用 `DEEPSEEK_API_KEY`，代码读 `dazuoye_api`（全仓 0 引用）~~ **已改**（变量名 + 补 MySQL service）；**⚠ 仍需你真跑一次 CI 确认变绿** | `.github/workflows/eval-gate.yml` | 修复详见 **十一 · 模块 C** | 📋 |
| E2 | 容器以 root 运行（无 `USER` 指令） | `Dockerfile` | 📋 |
| E3 | **无任何对外告警通道** | 全仓无 webhook / 邮件 | 告警只写库 + 推给**当时在线**的 WS → 店里没人开网页就等于丢弃 | 📋 |
| E4 | README 与实现不符（约 10 处） | `README.md` | 含"首启会打印 root 口令"（实际只在新库时）、`MYSQL_HOST` 文档说可改实际被硬编码 | 📋 |
| E5 | 无面向 compose 的备份/恢复流程与文档 | `README.md` | 只有迁移期的 `mysqldump` 说明 | 📋 |
| E6 | 基础镜像浮动 tag、依赖无锁定；qdrant 无 healthcheck 却用 `service_started` | `docker-compose.yml` | 构建不可复现 | 📋 |
| ✅ E7 | ~~mysql healthcheck 用 `mysqladmin ping`，只证明进程活着~~ **已修**（改为真实 `SELECT 1`；错误口令下探针 exit 1） | `docker-compose.yml` | 修复与验证见 **十一 · 模块 C** | 🔬 我实测（两种探针对照） |
| E8 | `TZ` 设了但未装 tzdata；mysql 服务未设 TZ | `Dockerfile`、`docker-compose.yml` | 同一行里 Python 时间与 `CURRENT_TIMESTAMP` 可能差 8 小时 | 📖 |
| E9 | 无指标端点、无结构化日志、无日志聚合 | 全仓 | 211 处 `print`；日志轮转已在 29.14 补上 | 📋 |

## F. 功能缺口（需要产品决策，不是 bug）

| # | 事项 | 说明 |
|---|---|---|
| F1 | 外部告警推送 | 企业微信机器人（最省事）或小程序订阅消息（最贴合现有架构，可推到个人微信） |
| F2 | 告警处理闭环 | 无"已处理/误报"状态 → 无法用误报率反哺阈值 |
| F3 | 多路摄像头的动线持久化 | 单路 WS 路径每 30 帧 drain 一次；多路引擎从不 drain（见 29.23），故多路无动线数据 |
| F4 | 营业时间感知 | 闭店时段"客流 0"是正常的，会污染报表均值与归因 |
| F5 | 变化驱动的 LLM 汇报 | 现在固定 10 分钟一轮 = 144 次/天/店；"无变化则跳过"可降一个量级成本 |
| F6 | pytest 单元测试 | 现有 CI 门禁只验"跑完没崩"，缺断言级单测 |
| F7 | 多进程化 | 需先把进程内状态外置（`_MAP_CACHE`、限流信号量、`_active_camera`、汇报基线、以及 29.14 的 WS 会话状态）；Redis 已常驻可承接 |
| F8 | 镜像瘦身 | 换 `opencv-python-headless` 去掉 Mesa GL/LLVM（约 100MB+），需处理 ultralytics 的依赖声明冲突 |
| F9 | per-connection 视频会话 | 彻底解决 29.19 的残留局限（同一用户两个标签页） |
| F10 | 登录会话与限流 | **刻意不进 Redis**：存在独立 SQLite 鉴权库，保证业务库故障时管理员仍能登录。若要改，需先权衡这个隔离性 |
