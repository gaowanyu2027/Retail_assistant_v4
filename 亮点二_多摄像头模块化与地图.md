# 亮点二：多摄像头模块化 + 百度地图（STAR）

## 一句话

> 把单摄像头 Demo 升级为**多摄像头按需加载模块**的零售 Agent 系统：按标签配置模块、运行时增删、数据隔离，并接入百度地图做竞品/商圈分析，前端能显性管理。

## S 背景

- 门店多摄像头（店内货架/门口/收银台）需求不同，却共用一个全局分析管线 → 无法区分镜头、数据串号
- 要接入外部地图服务（竞品/商圈/配送），但需要「按业务定制 + 保留通用扩展开关」

## T 任务

- 按摄像头位置**按需加载**分析模块（标签决定候选池，配置决定加载）
- 每镜头**独立数据**（数据隔离）
- 前端**显性按钮**管理（识别/添加/勾选模块/绑定视频）
- 地图：WebAPI 定制为主 + MCP 可选

## A 行动（我做的架构设计）

1. **模块抽象** `agents/analytics_module.py`：`AnalyticsModule`（process 喂数据 + get_stats + 工厂(roi)→独立实例）
2. **模块注册表** `agents/module_registry.py`：`Camera` 每镜头独立 roi + skill 实例 + `process_frame` 喂数据；运行时 `add_module/remove_module/set_enabled`；`set_active_camera` 绑定视频
3. **标签候选池**：`_TYPE_CANDIDATES`（indoor_shelf / entrance / checkout → 各自允许模块），加载模块校验候选池（配错拦截）
4. **5 个模块**：shelf_heat / anomaly_detect / emotion_experience + 新实现 footfall(客流计数) / empty_shelf(空货架补货)
5. **管理接口** `/api/cameras`：scan 识别 / 增删摄像头 / 运行时加载模块 / 开关 / 绑定视频 / 模块统计
6. **video 管线接模块**：`stream.py` 每帧把轨迹喂给 active 摄像头 `feed_current_frame`
7. **前端设置页**（小程序）：摄像头管理——识别/添加 + 勾选候选模块 + 绑定视频（显性按钮）
8. **百度地图**：`map_tools.py`(WebAPI 4 工具) + `mcp_maps.py`(MCP 14 工具可开关)

## R 结果（验证）

| 项 | 验证 |
|---|---|
| 标签候选池校验 | 门口装货架模块 → 400 拦截 |
| 数据隔离 | cam_in_01 喂轨迹后有值；cam_door 未喂 → 空/未启用 |
| 运行时加载/卸载 | footfall 加载成功、emotion 卸载/重载正常 |
| 新模块算法 | footfall 进店3/外出1/净客流2；empty_shelf 有访问不需补货 |
| 前端接口 | getCameras/getModules/scan/load/unload/bind 全通 |
| 地图 | 北京天安门 3km 70 家便利性 + 商圈 100 分 |

## 面试可讲的技术点

1. **为什么标签 + 配置双重决定**：标签定候选池（"这类镜头能装哪些"），配置定实际加载（"这个镜头装哪些"）——兼顾可扩展(+新模块不改注册表)与防错(候选池校验)
2. **数据隔离 = 每镜头独立 skill + 独立喂数据入口**，不是共享全局单例——摆脱"所有镜头算一份数据"
3. **运行时加载不重启**：ModuleRegistry 运行时增删/开关，前端接口驱动，符合生产可运营
4. **MCP vs WebAPI 选型**（引用百度地图官方建议）：定制业务(竞品/配送)用 WebAPI 深度定制；通用能力(天气/路况)用 MCP 按需叠加——"MCP 是快速会用，WebAPI 是真正掌控"
