# 模板路由劫持防御

> 智能零售分析系统的意图路由层，如何根治"模板路由劫持"问题。
> 相关代码：`agents/intent_router.py`（路由规则）、`agents/semantic_router.py`（向量语义闸）
> 回归保障：`evals/cases.json` 中的 meta_09/10/11 用例

---

## 问题背景

系统为高频数据查询（货架热度/告警/表情/汇报）设计了**零 LLM 模板路由**：
命中关键词即调用 skill 直接生成模板回答，把响应时间从秒级降到毫秒级。

模板判定的原始条件是"命中关键词即模板"，而"热度/客流/情绪"这类词会出现在
**任何语境**（游戏热度、天气热度、你的情绪、怎么提高热度）。真实口语问句一旦
含这些词，就被误判为数据查询，返回"当前最热的是2号货架…"之类答非所问的模板回答。

典型误判序列（真实对话截图暴露）：

| 类型 | 问句 | 错误返回 |
|---|---|---|
| 机制询问 | 你是通过销量跟热度做对比判断转换率吗 | 货架排行 ×2 |
| 影响因素 | 今天热度你觉得受什么影响？ | 货架排行 |
| 提升策略 | 怎么提高热度？给我一些建议 | 货架排行 |
| 人称元问题 | 你情绪怎么样？ | 顾客情绪统计 |
| 无关话题 | CS2热度怎么样 | 货架排行 |
| 归因口语 | 你觉得货架热度受啥影响 | 货架排行 |

---

## 演进历程（六轮）

### 第 1 轮：前置检测黑名单
在数据意图判定之前加 7 类前置检测（注入/归因/策略/人称/无关话题/机制/过去时间），命中即归 general 走 LLM。
**结果**：逐类封堵截图暴露的误判；但关键词是穷举式的——"受什么影响"拦住，"受啥影响"漏。

### 第 2 轮：强/弱词分级
数据词分级：**强词**（货架/排名/告警/表情…，语境专属）命中即模板；
**弱词**（热度/客流/受欢迎/情绪…，跨语境常见）需 ≥2 个才走模板，单独出现 → LLM。
**结果**：消除弱词单独劫持；但引入组合型回归（客流弱+告警强被误判单意图）。

### 第 3 轮：组合意图信号级判定
组合判定改用"信号级"（强或弱任一命中即算该意图有信号），多意图信号 ≥2 → general 交 LLM 融合。
**结果**：修复组合回归。

### 第 4 轮：结构正则替代关键词穷举
穷举口语变体是开放集（受什么/受啥/受嘛/受哪些…永远追不完）。
改为匹配**封闭的意图骨架**——归因/策略/观点/机制四类的中文问法结构有限，4 条正则一次全覆盖：

| 意图类 | 结构骨架 |
|---|---|
| 归因 | `受…影响`、`(什么\|啥\|嘛\|哪些)…(导致\|原因\|因素)`、`取决(于)…`、`(跟\|和\|与)…(有关\|关系)`、`靠/凭…` |
| 策略 | `(怎么\|如何\|咋\|怎样\|咋样\|怎么样)(才能)?…(提高\|提升\|改善\|优化\|安排…)` |
| 观点 | `你觉得\|你认为\|你看\|怎么看\|咋看` |
| 机制 | `怎么(算\|判断\|统计\|对比\|计算)`、`通过/用/靠…什么(方法\|原理\|数据)` |

**结果**：21 个口语变体单测全拦截、10 个模板报数问零误伤。

### 第 5 轮：向量语义路由（实证结论）
尝试用向量相似度做路由（Ollama bge-small-zh）。标定实证：
- **主题化负例库**：bge 按主题聚类，模板类"货架热度排名"相似度 0.878 ≈ 分析类"你觉得货架热度受啥影响"0.962，阈值无安全区
- **去主题化负例库**：模板类降到 ≤0.703，但 0.4-0.72 区间模板/分析完全重叠（"今天有没有可疑行为"0.682 是模板，"你觉得今天客流怎么样"0.633 是分析）
- **正例双信号**：正例与负例高度相关、无判别力（"咋回事儿"正例 0.663 > 负例 0.648）

**结论**：bge-small-zh 对中文短问句的语义区分度不足以做模板/分析裁决；
向量只能做**高置信兜底**，不能做裁决器。

### 第 6 轮：分层裁决架构（最终方案）
四层职责分离：

| 层 | 承担工作 | 不做什么 |
|---|---|---|
| 正则规则 | 否定剥离、强指令白名单；捕获 100% 确定模板 | 不处理口语/歧义/混合意图 |
| 负例向量 | 只抓相似度 >0.75 极典型分析句，输出风险标记 | 不做阻断硬闸 |
| 正例向量 | 仅输出相似度数值做辅助特征 | 不做阈值硬闸 |
| Agent LLM | 唯一路由决策者；处理口语/混合/否定；先调工具拿数据再归因 | 不重复简单正则 |

关键落地：
1. **否定分析剥离**：「不要分析货架热度受什么影响，直接告诉我货架热度」字面含完整归因问句、向量必误判，但用户真实意图是拒绝分析只要数据——规则层命中否定词即跳过分析检测走模板
2. **口语疑问词变体补全**：咋个/咋地/咋整/为啥子/啥情况/怎么个…（封闭小集合）封死 0.6-0.7 区间负例向量抓不到的漏网
3. **语义闸**：`semantic_router.analyze()` 输出 `sim_neg/sim_pos/risk` 双信号，决策只用 `risk`（sim_neg ≥ 0.75）

---

## 最终判定链（当前线上）

```
否定剥离 → 注入 → 分析结构正则(含口语变体) → 人称/无关/过去 → 数据分析域
  → 历史回顾 → 强/弱词+组合判定 → 模板候选 + 负例风险(>0.75) → 降级 LLM
  → 否则模板（毫秒级）| 其余全部 LLM 裁决
```

- 确定模板：强词命中 + 无任何分析形态（毫秒级，零 LLM）
- 确定分析：结构正则命中
- 模糊地带：默认 LLM（安全优先）
- Fail-open：Ollama 不可用时语义闸自动关闭，主链路不受影响

## 量化结果

| 指标 | 数值 |
|---|---|
| 评测集 | 80 条全量 100%（含 meta_09/10/11 防回归用例） |
| 模板拦截 | 31/31 保持毫秒级（~0.05s） |
| 口语变体 | 30+ 全拦截、模板零误伤 |
| 负例阈值 | 0.75（模板类最高 0.703，余量 0.047） |
| 主题化 vs 去主题化 | 模板类相似度 0.878 → 0.452 |

## 可复现

```bash
# 路由单测（口语变体 + 模板回归）
python -m py_compile agents/intent_router.py agents/semantic_router.py
# 全量评测（含 8 条防回归元用例）
python evals/run_evals.py
```
