# ==================== 零售视频分析系统 依赖清单 ====================
# 安装: pip install -r requirements.txt

# --- 深度学习 & 计算机视觉 ---
ultralytics>=8.0.0          # YOLO检测器
torch>=2.0.0                # PyTorch
torchvision>=0.15.0         # 视觉模型（ResNet50等）
opencv-python>=4.8.0        # 图像处理
numpy>=1.24.0               # 数值计算
scipy>=1.10.0               # 科学计算（马氏距离等）
filterpy>=1.4.5             # 卡尔曼滤波

# --- Web框架 ---
fastapi>=0.100.0            # FastAPI后端
uvicorn>=0.23.0             # ASGI服务器
websockets>=11.0            # WebSocket支持
pydantic>=2.0.0             # 数据验证

# --- 配置 & 数据 ---
pyyaml>=6.0                 # YAML配置文件解析

# --- AI/LLM客户端（Agent层，通过DeepSeek调用） ---
openai>=1.0.0               # DeepSeek走OpenAI兼容接口（必须）
langchain>=1.0              # LangChain框架（Agent编排，与travel_agent对齐）
langchain-openai>=1.0       # LangChain OpenAI兼容适配器
sherpa-onnx>=1.10.0         # 本地KWS唤醒词模型
edge-tts>=7.2.8             # 服务端中文语音合成
PyMySQL>=1.0.0              # MySQL连接驱动
qdrant-client>=1.19.0       # 本地向量检索

# --- 工具库 ---
loguru>=0.7                 # 结构化日志（替代print）
httpx>=0.27                 # 异步HTTP客户端
python-multipart>=0.0.9     # 文件上传支持

# --- 前端（通过CDN加载，无需本地安装） ---
# ECharts — CDN引入 (index.html 中已包含)
